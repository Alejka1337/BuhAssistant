# План доработок MVP 2.0 для eGlavBuh

**Дата создания:** 4 декабря 2025  
**Статус:** В разработке

---

## Общий обзор

MVP 2.0 включает 8 основных направлений доработок приложения eGlavBuh:

1. ✅ Push-уведомления для незарегистрированных пользователей (iOS)
2. ✅ Проверка механизма refresh tokens (iOS + Web)
3. ✅ Три новых калькулятора с расширенным функционалом
4. ✅ Два новых парсера для авторитетных источников
5. ✅ AI-модерация форума через OpenAI API
6. ✅ Раздел со статьями от авторов (только веб) - Завершено 7 декабря 2025
7. 📋 Реквизиты налоговых органов с поиском (только веб)
8. 💳 Генерация PDF платежек (только веб)

---

## Этап 1: Push-уведомления для незарегистрированных

### Backend

**Модель:** `backend/app/models/push_token.py`
```python
class AnonymousPushToken(Base):
    id, token, platform, device_id, created_at, last_active_at
```

**API:** `backend/app/api/push_notifications.py`
- POST `/api/push/register-anonymous` - регистрация анонимного токена
- POST `/api/push/link-to-user` - привязка к user при регистрации
- POST `/api/push/send-news` - рассылка новостей всем

**Логика рассылки:**
- Новости → всем (users + anonymous)
- Дедлайны → только зарегистрированным

### Frontend (iOS)

**Файлы:** `app/_layout.tsx`, `contexts/AuthContext.tsx`

**Логика:**
1. При первом запуске → запрос разрешения на push
2. Сохранить токен как анонимный
3. При регистрации/логине → перенести в user.push_token

---

## Этап 2: Проверка refresh tokens

### Backend
- Файл: `backend/app/api/auth.py`
- Проверить корректность обновления refresh token
- Обработка истекших токенов

### iOS
- Файл: `utils/authService.ts`
- Автоматический refresh при 401
- Retry запроса после refresh

### Web
- Файл: `utils/authService.ts`
- Axios interceptors для 401
- Обработка параллельных запросов

---

## Этап 3: Три новых калькулятора

### 3.1 Единый налог 4 группа
**Файл:** `components/SingleTaxGroup4Modal.tsx`
- Селекторы: тип деятельности, регион
- Инпуты: сумма дохода
- **Формулы:** ожидаем от бухгалтера

### 3.2 Калькулятор больничных
**Файл:** `components/SickLeaveCalculatorModal.tsx`
- Инпуты: средняя зарплата, количество дней, стаж
- Селекторы: причина (болезнь, травма, уход)
- **Формулы:** ожидаем от бухгалтера

### 3.3 Калькулятор декретных
**Файл:** `components/MaternityLeaveCalculatorModal.tsx`
- Инпуты: средняя зарплата, дата выхода
- Селекторы: тип декрета
- **Формулы:** ожидаем от бухгалтера

### Интеграция
- Добавить в `app/(tabs)/tools.tsx`
- Адаптация для веб-версии

---

## Этап 4: Два новых парсера

### 4.1 Парсер buhplatforma.com.ua
**Файл:** `backend/app/crawlers/buhplatforma_crawler.py`
- Парсинг через BeautifulSoup4
- Игнорирование модалок регистрации
- Извлечение: title, text, date, URL, image

### 4.2 Парсер 7eminar.ua
**Файл:** `backend/app/crawlers/7eminar_crawler.py`
- Аналогичная логика

### Интеграция
- Обновить `constants/sources.ts`
- Добавить Celery tasks в `backend/app/tasks/crawlers.py`
- Обновить enum NewsSource

---

## Этап 5: AI-модерация форума

### Backend

**Сервис:** `backend/app/services/moderation_service.py`
```python
async def check_content_with_ai(text: str) -> ModerationResult:
    # OpenAI Moderation API
    # Категории: spam, profanity, unrelated, gambling, adult
    # Return: approved/rejected + reason
```

**Модель:** `backend/app/models/moderation.py`
```python
class ModerationLog(Base):
    id, content_type, content_id, user_id
    decision, reason, ai_response, created_at
```

**API:** Обновить `backend/app/api/forum.py`
- POST `/api/forum/threads` - модерация перед созданием
- POST `/api/forum/posts` - модерация перед созданием

**Логика:**
1. Пользователь отправляет контент
2. Backend → OpenAI API
3. Rejected → 400 с причиной
4. Approved → сохранение в БД

### Frontend

**Файлы:** `app/forum/new-thread.tsx`, `app/forum/thread/[id].tsx`

При ошибке 400:
- Модальное окно "Контент не прошел модерацию"
- Показать причину
- Рекомендации по исправлению

---

## Этап 6: Статьи от авторов (только веб)

### Backend

**Модель:** `backend/app/models/article.py`
```python
class Article(Base):
    id, title, slug, content (HTML)
    excerpt, author_id, cover_image
    is_published, views, created_at, published_at
```

**API:** `backend/app/api/articles.py`
- GET `/api/articles` - список (публичный)
- GET `/api/articles/{slug}` - одна статья (публичный)
- POST `/api/articles` - создание (модераторы/админы)
- PUT `/api/articles/{id}` - редактирование
- DELETE `/api/articles/{id}` - удаление (админы)

### Frontend (Web only)

**Страницы:**
- `app/articles.tsx` - список статей (grid/list, фильтры, пагинация)
- `app/article/[slug].tsx` - отдельная статья (rich content)
- `app/admin/articles/new.tsx` - создание статьи

**Редактор:** Rich text (React Quill или TipTap)

**Навигация:** Добавить в sidebar (desktop) и burger menu (mobile web)

⚠️ **НЕ добавлять в iOS версию**

---

## Этап 7: Реквизиты налоговых органов (только веб)

### Backend

**Модель:** `backend/app/models/tax_details.py`
```python
class TaxDetails(Base):
    id, region_code, district_name
    recipient, recipient_code, bank, iban
    budget_code, budget_code_description
```

**Script:** `backend/scripts/load_tax_details.py`
- Чтение CSV для всех 25 областей
- Парсинг и валидация
- Bulk insert в БД

**API:** `backend/app/api/tax_details.py`
- GET `/api/tax-details/search?city={name}` - поиск
- GET `/api/tax-details/regions` - список областей
- DELETE `/api/tax-details/all` - удаление всех (админы)
- POST `/api/tax-details/upload-csv` - загрузка CSV (админы)

### Frontend (Web only)

**Страница:** `app/tax-details.tsx`
- Поле поиска по населенному пункту
- Селектор области
- Таблица результатов
- Кнопка "Створити платіжку" для каждой строки
- Пагинация

**Админ:** `app/admin/tax-details.tsx`
- Кнопка удаления всех реквизитов
- Загрузка CSV
- Статистика

⚠️ **НЕ добавлять в iOS версию**

---

## Этап 8: Генерация PDF платежек (только веб)

### Backend

**Шаблон:** `backend/templates/payment_template.docx`
- Плейсхолдеры: `{{period}}`, `{{year}}`, `{{inn}}`, `{{full_name}}`, `{{amount}}`, реквизиты

**Сервис:** `backend/app/services/payment_service.py`
```python
def generate_payment_pdf(data: PaymentData) -> bytes:
    # 1. python-docx: заполнение шаблона
    # 2. subprocess: конвертация DOCX → PDF через LibreOffice
    # 3. Возврат PDF bytes
```

**API:** `backend/app/api/payments.py`
- POST `/api/payments/generate-pdf`

**Docker:** Добавить LibreOffice в `backend/Dockerfile`

### Frontend (Web only)

**Модалка:** `components/CreatePaymentModal.tsx`
- Селектор периода (месяц 1-12, квартал, полугодие, 9 мес, год)
- Инпуты: год, ИНН, ФИО, сумма
- Автоподстановка реквизитов
- Превью данных
- Кнопка "Створити та завантажити PDF"

**Интеграция:** В `app/tax-details.tsx`
- Кнопка для каждой строки таблицы
- Открытие модалки
- Скачивание PDF

**Сервис:** `utils/paymentService.ts`

---

## Приоритизация

### Высокий приоритет
1. **Этап 1** - Push для незарегистрированных
2. **Этап 5** - AI-модерация (критично для App Store)
3. **Этап 2** - Проверка refresh tokens

### Средний приоритет
4. **Этап 3** - Калькуляторы (ждем формулы от бухгалтера)
5. **Этап 4** - Новые парсеры

### Низкий приоритет
6. **Этап 6** - Статьи (только веб)
7. **Этап 7** - Реквизиты (только веб)
8. **Этап 8** - PDF платежки (только веб)

---

## Зависимости

- **Этап 3** заблокирован до получения формул от бухгалтера
- **Этап 8** требует завершения **Этапа 7** (нужны реквизиты)
- **Этап 5** требует API ключ OpenAI

---

## Технические требования

### Backend
- Python 3.11+
- FastAPI
- SQLAlchemy + Alembic
- BeautifulSoup4 (парсинг)
- python-docx (генерация DOCX)
- LibreOffice (конвертация PDF)
- OpenAI API (модерация)

### Frontend
- React Native + Expo (iOS)
- React (Web)
- React Quill / TipTap (Rich text editor)

### Deployment
- Docker + Docker Compose
- PostgreSQL
- Redis (Celery)
- Nginx

---

## Миграции Alembic

1. `anonymous_push_tokens` - хранение анонимных токенов
2. `moderation_logs` - логи AI-модерации
3. `articles` - статьи от авторов
4. `tax_details` - реквизиты налоговых органов

---

## Тестирование

Для каждого этапа:
1. Unit тесты для backend
2. Ручное тестирование iOS / Web
3. Edge cases (особенно AI модерация, PDF генерация)

---

## Timeline (примерный)

| Этап | Время | Зависимости |
|------|-------|-------------|
| Этап 1 | 3-4 дня | - |
| Этап 2 | 1-2 дня | - |
| Этап 3 | 4-5 дней | Формулы от бухгалтера |
| Этап 4 | 2-3 дня | - |
| Этап 5 | 3-4 дня | OpenAI API key |
| Этап 6 | 5-6 дней | - |
| Этап 7 | 4-5 дней | CSV файлы с tax.gov.ua |
| Этап 8 | 3-4 дня | Этап 7 завершен |

**Общее время:** ~25-35 дней

---

## Deployment на production

### После каждого этапа:
1. Применить миграции Alembic
2. Обновить environment variables
3. Перезапустить backend/frontend
4. Создать новый build для iOS (если нужно)

### Финальный деплой:
1. Полное тестирование всех функций
2. Обновить `buildNumber` в `app.json`
3. Создать production build
4. Отправить в App Store для ревью

---

## Контакты и документация

- **Backend API:**  http://localhost:8000/api/docs
- **Frontend Web:** http://localhost:8081
- **Документация:** `/doc-files/`, `/docs/`

---

**Статус:** План утвержден, готов к реализации 🚀

