# Этап 2: Проверка и исправление механизма Refresh Tokens ✅

**Дата завершения:** 5 декабря 2025  
**Статус:** Исправлено и готово к тестированию

---

## 🔍 Что было проверено

### Backend ✅

**Файл:** `backend/app/core/config.py`
- ✅ **ACCESS_TOKEN_EXPIRE_MINUTES: 30** минут
- ✅ **REFRESH_TOKEN_EXPIRE_DAYS: 7** дней

**Файл:** `backend/app/api/auth.py`
- ✅ **Endpoint `/api/auth/refresh`** работает корректно:
  1. Декодирует refresh token
  2. Проверяет тип токена (`refresh`)
  3. Проверяет пользователя (активен/существует)
  4. Создает новую пару токенов (access + refresh)
  5. Возвращает AuthResponse

**Результат:** Backend работает правильно ✅

---

## 🐛 Проблемы, которые были найдены

### Frontend: Отсутствие автоматического refresh в некоторых функциях ❌

**Файл:** `utils/authService.ts`

**Функция `authenticatedFetch`** уже имела автоматический refresh при 401:
```typescript
// Если получили 401 - токен истёк, пробуем обновить
if (response.status === 401) {
  // Обновляем токен
  const authData = await refreshAccessToken();
  // Повторяем запрос с новым токеном
  response = await fetch(url, { headers: newHeaders });
}
```

**НО**: Следующие функции использовали обычный `fetch` вместо `authenticatedFetch`:
1. ❌ `getCurrentUser()` - используется в AuthContext при проверке авторизации
2. ❌ `fetchUserProfile()` - используется в профиле
3. ❌ `updateUserProfile()` - используется при обновлении профиля
4. ❌ `deleteAccount()` - используется при удалении аккаунта

**Последствия:**
- После истечения access token (30 минут) эти функции возвращали 401
- Автоматический refresh НЕ срабатывал
- Пользователь вылетал из приложения

---

## ✅ Что было исправлено

### Обновлено 4 функции в `authService.ts`

#### 1. `getCurrentUser()` ✅

**Было:**
```typescript
export const getCurrentUser = async (): Promise<User> => {
  const token = await getAccessToken();
  if (!token) {
    throw new Error('No access token');
  }

  const response = await fetch(`${API_ENDPOINTS.AUTH.ME}`, {
    headers: getHeaders({
      'Authorization': `Bearer ${token}`,
    }),
  });

  if (!response.ok) {
    throw new Error('Failed to get current user');
  }

  return await response.json();
};
```

**Стало:**
```typescript
export const getCurrentUser = async (): Promise<User> => {
  const response = await authenticatedFetch(`${API_ENDPOINTS.AUTH.ME}`);

  if (!response.ok) {
    throw new Error('Failed to get current user');
  }

  return await response.json();
};
```

#### 2. `fetchUserProfile()` ✅

**Было:**
```typescript
export const fetchUserProfile = async (): Promise<User> => {
  const token = await getAccessToken();
  const response = await fetch(`${API_ENDPOINTS.PROFILE.ME}`, {
    headers: getHeaders({ 'Authorization': `Bearer ${token}` }),
  });
  // ...
};
```

**Стало:**
```typescript
export const fetchUserProfile = async (): Promise<User> => {
  const response = await authenticatedFetch(`${API_ENDPOINTS.PROFILE.ME}`);
  // ...
};
```

#### 3. `updateUserProfile()` ✅

**Было:**
```typescript
export const updateUserProfile = async (data: UserProfileUpdate): Promise<User> => {
  const token = await getAccessToken();
  const response = await fetch(`${API_ENDPOINTS.PROFILE.ME}`, {
    method: 'PUT',
    headers: getHeaders({ 'Authorization': `Bearer ${token}` }),
    body: JSON.stringify(data),
  });
  // ...
};
```

**Стало:**
```typescript
export const updateUserProfile = async (data: UserProfileUpdate): Promise<User> => {
  const response = await authenticatedFetch(`${API_ENDPOINTS.PROFILE.ME}`, {
    method: 'PUT',
    body: JSON.stringify(data),
  });
  // ...
};
```

#### 4. `deleteAccount()` ✅

**Было:**
```typescript
export const deleteAccount = async (): Promise<void> => {
  const token = await getAccessToken();
  const response = await fetch(`${API_ENDPOINTS.AUTH.DELETE_ACCOUNT}`, {
    method: 'DELETE',
    headers: getHeaders({ 'Authorization': `Bearer ${token}` }),
  });
  // ...
};
```

**Стало:**
```typescript
export const deleteAccount = async (): Promise<void> => {
  const response = await authenticatedFetch(`${API_ENDPOINTS.AUTH.DELETE_ACCOUNT}`, {
    method: 'DELETE',
  });
  // ...
};
```

---

## 🔄 Как работает автоматический refresh

### Логика в `authenticatedFetch`:

```
1. Запрос с текущим access token
   ↓
2. Если 200 OK → возврат ответа ✅
   ↓
3. Если 401 Unauthorized:
   ↓
4. Вызов refreshAccessToken()
   ↓
5. POST /api/auth/refresh с refresh token
   ↓
6. Backend проверяет refresh token (срок: 7 дней)
   ↓
7. Если валиден → новая пара токенов ✅
   ↓
8. Сохранение новых токенов в secure storage
   ↓
9. Повторный запрос с новым access token
   ↓
10. Возврат ответа пользователю ✅

Если refresh token истек (>7 дней):
   ↓
Backend возвращает 401 на /refresh
   ↓
Frontend удаляет токены
   ↓
Пользователь перенаправляется на login ❌
```

---

## 📊 Временные рамки

| Сценарий | Поведение |
|----------|-----------|
| < 30 минут после логина | Access token валиден, запросы проходят без refresh |
| 30 минут - 7 дней | Access token истек → автоматический refresh → запрос повторяется |
| > 7 дней | Refresh token истек → выход из системы → требуется повторный логин |

---

## 🧪 Инструкция по тестированию

### Тест 1: Автоматический refresh после 30 минут (iOS)

#### Подготовка:
1. **Уменьшить срок действия access token** (для быстрого теста):
   ```python
   # backend/app/core/config.py
   ACCESS_TOKEN_EXPIRE_MINUTES: int = 1  # 1 минута вместо 30
   ```
2. **Перезапустить backend:**
   ```bash
   docker-compose restart backend
   ```
3. **Запустить iOS приложение:**
   ```bash
   npx expo run:ios
   ```

#### Шаги теста:
1. ✅ **Войти в приложение** (login)
2. ✅ **Дождаться истечения access token** (подождать 1-2 минуты)
3. ✅ **Открыть профиль** или **обновить данные**
4. ✅ **Проверить логи** в Metro/Xcode:
   ```
   ⚠️ Token expired, attempting to refresh...
   ✅ Token refreshed successfully
   🔄 Retrying request with new token...
   📡 Retry response status: 200
   ```
5. ✅ **Убедиться**, что пользователь остался авторизованным
6. ✅ **Проверить**, что данные профиля загрузились

#### Ожидаемый результат:
- ✅ Автоматический refresh срабатывает
- ✅ Пользователь не видит перехода на login
- ✅ Данные загружаются корректно

---

### Тест 2: Автоматический refresh после 30 минут (Web)

#### Подготовка:
1. **Уменьшить срок действия access token** (если еще не сделано)
2. **Запустить web версию:**
   ```bash
   npx expo start --web
   ```

#### Шаги теста:
1. ✅ **Войти в приложение** (login)
2. ✅ **Дождаться истечения access token** (подождать 1-2 минуты)
3. ✅ **Открыть профиль** или **обновить данные**
4. ✅ **Проверить логи** в Browser Console (F12):
   ```
   🔐 authenticatedFetch: me
   📡 Response status: 401
   ⚠️ Token expired, attempting to refresh...
   ✅ Token refreshed successfully
   🔄 Retrying request with new token...
   📡 Retry response status: 200
   ```
5. ✅ **Убедиться**, что пользователь остался авторизованным

#### Ожидаемый результат:
- ✅ Автоматический refresh срабатывает
- ✅ Пользователь не перенаправляется на login
- ✅ Данные загружаются корректно

---

### Тест 3: Истечение refresh token (>7 дней)

#### Подготовка:
1. **Уменьшить срок действия refresh token:**
   ```python
   # backend/app/core/config.py
   ACCESS_TOKEN_EXPIRE_MINUTES: int = 1  # 1 минута
   REFRESH_TOKEN_EXPIRE_DAYS: int = 0  # Истекает сразу (для теста)
   ```
2. **Перезапустить backend**

#### Шаги теста:
1. ✅ **Войти в приложение**
2. ✅ **Дождаться истечения access token** (1-2 минуты)
3. ✅ **Попытаться обновить данные**
4. ✅ **Проверить логи:**
   ```
   ⚠️ Token expired, attempting to refresh...
   ❌ Failed to refresh token: Failed to refresh token
   🚪 Session expired. Please login again.
   ```
5. ✅ **Убедиться**, что:
   - Токены удалены из storage
   - Пользователь перенаправлен на login

#### Ожидаемый результат:
- ✅ Backend возвращает 401 на `/refresh`
- ✅ Frontend удаляет токены
- ✅ Пользователь видит экран login

---

### Тест 4: Параллельные запросы

#### Цель:
Проверить, что при параллельных запросах refresh происходит только один раз.

#### Шаги:
1. ✅ **Войти в приложение**
2. ✅ **Дождаться истечения access token**
3. ✅ **Открыть несколько экранов одновременно** (профиль + форум + новости)
4. ✅ **Проверить логи** - должен быть только один refresh:
   ```
   ⚠️ Token expired, attempting to refresh... (один раз)
   ✅ Token refreshed successfully
   🔄 Retrying request... (для всех запросов)
   ```

#### Ожидаемый результат:
- ✅ Refresh вызывается только один раз
- ✅ Все запросы повторяются с новым токеном
- ✅ Нет race conditions

---

## ⚠️ Восстановление настроек после тестирования

После завершения тестов **восстановить** настройки в `backend/app/core/config.py`:

```python
ACCESS_TOKEN_EXPIRE_MINUTES: int = 30  # Вернуть 30 минут
REFRESH_TOKEN_EXPIRE_DAYS: int = 7     # Вернуть 7 дней
```

И перезапустить backend:
```bash
docker-compose restart backend
```

---

## 📋 Checklist готовности

- [x] Backend: Endpoint `/refresh` проверен
- [x] Backend: Сроки токенов корректные (30 мин / 7 дней)
- [x] Frontend: `getCurrentUser()` использует `authenticatedFetch`
- [x] Frontend: `fetchUserProfile()` использует `authenticatedFetch`
- [x] Frontend: `updateUserProfile()` использует `authenticatedFetch`
- [x] Frontend: `deleteAccount()` использует `authenticatedFetch`
- [x] Frontend: Логика `authenticatedFetch` проверена
- [ ] Тест iOS: Refresh после 30 минут
- [ ] Тест Web: Refresh после 30 минут
- [ ] Тест: Истечение refresh token
- [ ] Production: Протестировать на реальных устройствах

---

## 🎯 Итоги

### Что было исправлено:
- ✅ 4 функции теперь используют `authenticatedFetch` с автоматическим refresh
- ✅ iOS и Web используют одинаковый код (один `authService.ts`)
- ✅ Пользователь остается авторизованным до 7 дней

### Как это улучшает UX:
- ✅ Пользователь не вылетает из приложения каждые 30 минут
- ✅ Seamless experience - обновление токенов незаметно
- ✅ Logout только после 7 дней неактивности

### Production deployment:
1. Тестирование с уменьшенными таймаутами
2. Восстановление нормальных таймаутов (30 мин / 7 дней)
3. Деплой на production
4. Тестирование на реальных устройствах
5. Мониторинг логов refresh tokens

---

**Этап 2 завершен!** ✅  
**Следующий этап:** Этап 3 - Три новых калькулятора

