# План работы до релиза BuhAssistant в App Store и Google Play

## Этап 1: Улучшение UI/UX и исправление багов

### 1.1 Закрытие клавиатуры в калькуляторах

**Файлы:** `components/*Modal.tsx` (EsvCalculatorModal, SingleTaxGroup1Modal, и т.д.)

- Добавить `Keyboard.dismiss()` при нажатии на кнопку "Розрахувати"
- Импортировать `Keyboard` из `react-native`
- Применить ко всем 7 модалям калькуляторов

### 1.2 Обновление цветовой схемы

**Цель:** Сделать более насыщенный зеленый цвет и единообразный темный фон

**Изменения в `constants/Colors.ts`:**

- Основной зеленый: `#00bfa5` → `#00d4a8` (более насыщенный)
- Основной фон: `#1a1d21` (темный)
- Фон карточек: `#2c3e50` → `#22262c` (как в блоке "Найближчі звіти")
- Header фон: `#1a1d21` (единый темный)

**Файлы для обновления:**

- `app/(tabs)/_layout.tsx` - header цвета для всех табов
- `app/(tabs)/index.tsx` - уже правильные стили блока reports
- `app/(tabs)/calendar.tsx` - применить фон `#22262c` для карточек
- `app/(tabs)/search.tsx` - обновить стили результатов поиска
- `app/(tabs)/tools.tsx` - обновить стили плиток инструментов
- `app/(tabs)/forum.tsx` - применить единую цветовую схему (после создания)
- `app/(tabs)/profile.tsx` - обновить карточки и секции
- `app/news.tsx` - применить `#22262c` для карточек новостей
- `app/login.tsx`, `app/register.tsx` - обновить акцентные цвета
- Все компоненты калькуляторов - обновить зеленый цвет кнопок
- `components/AccountingCalendar.tsx` - обновить стили
- `components/SearchResultCard.tsx` - обновить стили

### 1.3 Унификация стилей header

- Все header должны иметь `backgroundColor: '#1a1d21'`
- Цвет текста заголовков: `#ecf0f1`
- Цвет акцентов (иконки, активные элементы): `#00d4a8`
- Применить во всех Stack.Screen и Tab.Screen

## Этап 2: Форум (Полный функционал)

### 2.1 Backend - База данных и модели

**Файл:** `backend/app/models/forum.py` (создать)

Модели:

- `ForumCategory` - категории форума (id, name, description, icon, order)
- `ForumThread` - топики (id, category_id, user_id, title, content, created_at, updated_at, views, is_pinned, is_closed)
- `ForumPost` - сообщения/комментарии (id, thread_id, user_id, parent_id, content, created_at, updated_at, edited_at)
- `ForumLike` - лайки (id, post_id, user_id, created_at)

**Файл:** `backend/migrations/versions/xxx_create_forum_tables.py`

- Создать миграцию Alembic для всех таблиц форума
- Добавить индексы для оптимизации (category_id, user_id, thread_id, parent_id)

### 2.2 Backend - Pydantic схемы

**Файл:** `backend/app/schemas/forum.py` (создать)

Схемы:

- `ForumCategoryResponse`, `ForumThreadCreate`, `ForumThreadResponse`, `ForumThreadListResponse`
- `ForumPostCreate`, `ForumPostUpdate`, `ForumPostResponse`
- `ForumLikeResponse`, `ThreadsFilterParams` (enum для сортировки: new, hot, unanswered)

### 2.3 Backend - API endpoints

**Файл:** `backend/app/api/forum.py` (создать)

Endpoints:

- `GET /api/forum/categories` - список категорий
- `GET /api/forum/threads?category_id={id}&sort={new|hot|unanswered}&limit={int}&offset={int}` - список топиков
- `GET /api/forum/threads/{id}` - детали топика + все комментарии (древовидная структура)
- `POST /api/forum/threads` - создание топика (protected)
- `PUT /api/forum/threads/{id}` - редактирование своего топика (protected)
- `DELETE /api/forum/threads/{id}` - удаление своего топика (protected)
- `POST /api/forum/posts` - создание комментария (protected)
- `PUT /api/forum/posts/{id}` - редактирование своего комментария (protected)
- `DELETE /api/forum/posts/{id}` - удаление своего комментария (protected)
- `POST /api/forum/likes` - лайк/анлайк (protected, toggle механизм)
- `GET /api/forum/search?q={query}` - поиск по топикам и комментариям

Добавить router в `backend/app/main.py`

### 2.4 Backend - Elasticsearch индексы

**Файл:** `backend/app/services/forum_search.py` (создать)

- Создать индекс `forum_threads` с полями: title, content, category_name
- Создать индекс `forum_posts` с полями: content, thread_title
- Функции: `index_thread()`, `index_post()`, `search_forum()`

### 2.5 Frontend - Экран форума (список)

**Файл:** `app/(tabs)/forum.tsx` (создать)

- Список категорий (горизонтальный скролл или сетка)
- Список топиков с фильтрами (TabView: Нові, Гарячі, Без відповіді)
- Карточка топика: заголовок, автор, дата, количество комментариев, лайков
- Бесконечный скролл (pagination)
- Pull-to-refresh
- FAB кнопка "Створити топік" (для авторизованных)
- Поиск (иконка в header)

**Стили:** темный фон `#1a1d21`, карточки `#22262c`, акценты `#00d4a8`

### 2.6 Frontend - Создание топика

**Файл:** `app/forum/create-thread.tsx` (создать)

- Выбор категории (Picker)
- Поле заголовка (TextInput)
- Поле содержания (TextInput multiline)
- Кнопка "Опублікувати"
- Валидация полей
- Loading state при создании

### 2.7 Frontend - Детальный просмотр топика

**Файл:** `app/forum/thread/[id].tsx` (создать)

- Отображение топика: заголовок, содержание, автор, дата, лайки
- Древовидная структура комментариев (до 3 уровней вложенности)
- Каждый комментарий: содержание, автор, дата, кнопка "Відповісти", кнопка лайка
- Поле для нового комментария внизу
- Кнопки редактирования/удаления для своих постов
- Обработка вложенности через отступы

### 2.8 Frontend - Компоненты

**Файлы создать:**

- `components/forum/CategoryCard.tsx` - карточка категории
- `components/forum/ThreadCard.tsx` - карточка топика
- `components/forum/PostItem.tsx` - комментарий (рекурсивный компонент для вложенности)
- `components/forum/LikeButton.tsx` - кнопка лайка с счетчиком
- `components/forum/SearchModal.tsx` - модальное окно поиска

### 2.9 Frontend - Сервисы

**Файл:** `utils/forumService.ts` (создать)

- Все функции для работы с API форума
- `getCategories()`, `getThreads()`, `getThreadById()`, `createThread()`, `createPost()`, `toggleLike()`, `searchForum()`

## Этап 3: Парсеры на Playwright ✅ ЗАВЕРШЕНО

### 3.1 Настройка Playwright ✅

**Статус:** ✅ Завершено

**Файлы обновлены:**
- `backend/requirements.txt` - добавлен `playwright==1.40.0`
- `backend/Dockerfile` - установлены системные зависимости и Chromium

```dockerfile
# Установка Chromium для appuser
USER appuser
COPY . .
RUN playwright install chromium
```

### 3.2 Tax.gov.ua парсер (Playwright) ✅

**Статус:** ✅ Завершено

**Файл:** `backend/app/crawlers/tax_gov_ua_playwright.py`

- ✅ Playwright с headless браузером
- ✅ Обход CDN защиты (wait for network idle)
- ✅ Парсинг структуры: заголовок, дата, содержание, URL
- ✅ Фильтрация через OpenAI
- ✅ Target: `https://tax.gov.ua/media-tsentr/novini/`
- ✅ Интеграция с Celery task (`crawl_tax_gov_ua_playwright_task`)
- ✅ API endpoint: `POST /api/news/crawl/tax-gov-ua-playwright`

**Результаты тестирования:**
- Parsed: 12 articles
- Filtered by OpenAI: 5 articles
- Saved to DB: 5 articles

### 3.3 Zakon.rada.gov.ua парсер (Playwright, опционально) ⏸️

**Статус:** ⏸️ Отложено

Причина: Приоритет на основных источниках новостей. Можно добавить позже при необходимости.

### 3.4 Diia.gov.ua парсер (Playwright) ✅

**Статус:** ✅ Завершено

**Файл:** `backend/app/crawlers/diia_gov_ua_playwright.py`

- ✅ Playwright для SPA приложения
- ✅ Target: `https://diia.gov.ua/news`
- ✅ Фильтр: только для бизнеса и ФОП
- ✅ Интеграция с Celery task (`crawl_diia_gov_ua_playwright_task`)
- ✅ API endpoint: `POST /api/news/crawl/diia-gov-ua-playwright`

**Результаты тестирования:**
- Parsed: 6 articles
- Filtered by OpenAI: 1 article
- Saved to DB: 1 article

### 3.5 Dtkt.ua парсер (BS4 + aiohttp) ✅

**Статус:** ✅ Завершено

**Файл:** `backend/app/crawlers/dtkt_crawler.py`

- ✅ BS4 + aiohttp парсер
- ✅ Target: `https://news.dtkt.ua/?sort=main`
- ✅ Парсинг украинских дат ("Сьогодні", "Вчора", DD.MM.YYYY)
- ✅ Интеграция с Celery task (`crawl_dtkt_task`)
- ✅ API endpoint: `POST /api/news/crawl/dtkt`

**Результаты тестирования:**
- Parsed: 36 articles
- Filtered by OpenAI: 25 articles
- Saved to DB: 25 articles (отличный результат!)

### 3.6 Интеграция в Celery ✅

**Статус:** ✅ Завершено

**Файл:** `backend/app/tasks/crawler_tasks.py`

**Добавлено 3 новых tasks:**
1. `crawl_tax_gov_ua_playwright_task` - Tax.gov.ua через Playwright
2. `crawl_diia_gov_ua_playwright_task` - Diia.gov.ua через Playwright
3. `crawl_dtkt_task` - Dtkt.ua через BS4

**Обновлен главный task:**
- `crawl_all_news_sources_task` - теперь парсит 6 источников:
  1. Minfin.com.ua (BS4)
  2. Liga.net (BS4)
  3. Buhgalter911.com (BS4)
  4. Tax.gov.ua (Playwright) ⭐
  5. Diia.gov.ua (Playwright) ⭐
  6. Dtkt.ua (BS4) ⭐

**Автоматическое расписание:**
- 2 раза в день: 8:00 и 20:00 UTC (10:00 и 22:00 Киев)
- Через Celery Beat scheduler

### 📊 Итоги Этапа 3:

**✅ Создано файлов:**
1. `backend/app/crawlers/tax_gov_ua_playwright.py`
2. `backend/app/crawlers/diia_gov_ua_playwright.py`
3. `backend/app/crawlers/dtkt_crawler.py`
4. `backend/CELERY_PARSERS_INTEGRATION.md` (документация)

**✅ Всего источников новостей: 6**
- 3 классических (BS4 + aiohttp): minfin, liga, buhgalter911
- 2 Playwright (для обхода CDN): tax.gov.ua, diia.gov.ua
- 1 новый BS4: dtkt.ua

**✅ Автоматизация:**
- ✅ Celery tasks для каждого источника
- ✅ Celery Beat scheduler (2 запуска в день)
- ✅ API endpoints для ручного запуска

**✅ Production ready:**
- ✅ Docker + Playwright setup
- ✅ Логирование
- ✅ Error handling
- ✅ Проверка дубликатов
- ✅ OpenAI фильтрация
- ✅ Async/await поддержка

**🎉 Этап 3 полностью завершен!**

---
- URL: нужно определить структуру сайта
- OpenAI фильтрация

### 3.6 Обновление Celery tasks

**Файл:** `backend/app/tasks/crawlers.py`

- Добавить новые краулеры в `crawl_all_news_sources_task`
- Настроить расписание для новых источников

**Файл:** `backend/app/celery_app.py`

- Убедиться что все новые таски зарегистрированы

## Этап 4: Деплой backend на AWS EC2

### 4.1 Подготовка AWS инфраструктуры

**Ресурсы:**

- **EC2 instance:** t3.medium или t3.large (2-4 vCPU, 4-8GB RAM)
- **RDS PostgreSQL:** db.t3.micro или db.t3.small для начала
- **ElastiCache Redis:** cache.t3.micro для Celery
- **S3 bucket:** (опционально) для статических файлов, если понадобится
- **CloudFront:** (опционально) для ускорения API если будет много запросов

**Security Groups:**

- Открыть порты: 80 (HTTP), 443 (HTTPS), 22 (SSH только для вашего IP)
- RDS: доступ только с EC2 instance
- Redis: доступ только с EC2 instance

### 4.2 Настройка EC2 instance

**Действия:**

1. Создать EC2 instance (Ubuntu 22.04 LTS)
2. Установить Docker и Docker Compose
3. Настроить Elastic IP (статический IP)
4. Настроить SSL сертификат (Let's Encrypt через Certbot)
5. Установить Nginx как reverse proxy

**Файлы создать:**

- `backend/docker-compose.prod.yml` - production конфигурация
- `backend/.env.production` - production переменные окружения
- `backend/nginx.conf` - конфигурация Nginx

### 4.3 Настройка RDS PostgreSQL

1. Создать RDS instance PostgreSQL 15
2. Настроить автоматические бэкапы (snapshot каждый день)
3. Получить connection string
4. Обновить `.env.production` с RDS credentials

### 4.4 Настройка ElastiCache Redis

1. Создать Redis cluster
2. Получить endpoint
3. Обновить `.env.production` с Redis endpoint

### 4.5 Deployment скрипты

**Файл:** `backend/deploy.sh` (создать)

```bash
#!/bin/bash
# Скрипт для деплоя на EC2
# - git pull
# - docker-compose down
# - docker-compose build
# - docker-compose up -d
# - alembic migrate
```

**Файл:** `backend/scripts/health_check.sh` (создать)

- Проверка здоровья API
- Проверка Celery workers
- Проверка Redis connection
- Проверка PostgreSQL connection

### 4.6 CI/CD (опционально для будущего)

**GitHub Actions workflow:**

- Автоматический deploy при push в `main` branch
- Запуск тестов перед деплоем
- Rollback при ошибках

## Этап 5: Тестирование на Android

### 5.1 Сборка Android приложения

**Команды:**

```bash
npx expo prebuild --platform android --clean
cd android
./gradlew assembleRelease
```

### 5.2 Тестирование функциональности

**Чек-лист:**

- [ ] Регистрация и логин
- [ ] Календарь отчетов (загрузка данных)
- [ ] Новости (загрузка, открытие статей)
- [ ] Инструменты (все калькуляторы)
- [ ] Поиск документов
- [ ] Форум (создание, комментирование, лайки)
- [ ] Профиль (редактирование, настройки уведомлений)
- [ ] Push уведомления (регистрация, получение)
- [ ] Забыли пароль (восстановление)

### 5.3 Исправление Android-специфичных багов

**Возможные проблемы:**

- Проблемы с клавиатурой (KeyboardAvoidingView)
- Стили SafeAreaView
- Производительность ScrollView/FlatList
- Шрифты и иконки
- Навигация (анимации, back button)

**Файлы для проверки:**

- `android/app/build.gradle` - версии SDK, permissions
- `app.json` - Android-специфичные настройки
- Все экраны с формами и вводом

## Этап 6: Google Play регистрация и подготовка

### 6.1 Создание Google Play Developer аккаунта

**Стоимость:** $25 (один раз)

**Действия:**

1. Зарегистрироваться на https://play.google.com/console
2. Оплатить регистрационный взнос
3. Заполнить профиль разработчика
4. Указать контактные данные

### 6.2 Подготовка материалов для Play Store

**Необходимо подготовить:**

**Иконки и скриншоты:**
- Иконка приложения 512x512 PNG
- Feature graphic 1024x500 PNG
- Скриншоты (минимум 2): 1080x1920 или 1080x2340 (разные экраны)
  - Главный экран
  - Календарь
  - Калькуляторы
  - Новости
  - Форум

**Тексты:**
- Название приложения (до 50 символов): "BuhAssistant - Бухгалтерський помічник"
- Краткое описание (до 80 символов)
- Полное описание (до 4000 символов)
- Что нового (release notes)

**Пример краткого описания:**
```
Податковий календар, калькулятори, новини і форум для бухгалтерів та ФОП
```

**Категория:** Business или Finance

**Возрастной рейтинг:** Everyone (3+)

**Контент:**
- Privacy Policy URL (создать страницу)
- Terms of Service URL (опционально)

### 6.3 Privacy Policy

**Файл:** создать `privacy-policy.html` и разместить на хостинге

**Содержание:**
- Какие данные собираются (email, имя, тип пользователя)
- Как используются (персонализация новостей, уведомления)
- Что не собирается (финансовые данные, платежи)
- Push уведомления (можно отключить)
- Контакты для вопросов

**Хостинг:** можно разместить на GitHub Pages или в S3 bucket

### 6.4 Подготовка APK/AAB

**Генерация signing key:**

```bash
cd android/app
keytool -genkeypair -v -storetype PKCS12 -keystore buhassistant-release.keystore -alias buhassistant -keyalg RSA -keysize 2048 -validity 10000
```

**Файл:** `android/gradle.properties` (добавить signing config)

**Сборка release AAB:**

```bash
cd android
./gradlew bundleRelease
```

**Результат:** `android/app/build/outputs/bundle/release/app-release.aab`

## Этап 7: Apple App Store релиз

### 7.1 Финализация Apple Developer Account

**Статус:** Аккаунт оплачен, ждем активации (до 2 рабочих дней)

**После активации:**
1. Заполнить все данные в App Store Connect
2. Создать App ID для BuhAssistant
3. Настроить Certificates, Identifiers & Profiles
4. Создать Provisioning Profile для production

### 7.2 Подготовка материалов для App Store

**Необходимо подготовить:**

**Скриншоты (для всех размеров iPhone):**
- iPhone 6.7" (1290x2796) - минимум 3 скриншота
- iPhone 6.5" (1242x2688) - минимум 3 скриншота
- iPhone 5.5" (1242x2208) - минимум 3 скриншота

**Рекомендуемые экраны для скриншотов:**
1. Главный экран с календарем
2. Калькуляторы
3. Новости
4. Форум

**Тексты:**
- Название (до 30 символов): "BuhAssistant"
- Subtitle (до 30 символов): "Помічник бухгалтера"
- Description (до 4000 символов)
- Keywords (до 100 символов): "бухгалтерія,податки,ФОП,калькулятор,звітність"
- What's New (до 4000 символов)

**Категории:**
- Primary: Business
- Secondary: Finance

**Иконка:**
- 1024x1024 PNG без прозрачности
- Без скругленных углов (iOS сделает автоматически)

### 7.3 Настройка push уведомлений в App Store Connect

1. Включить Push Notifications capability
2. Загрузить APNs сертификаты
3. Настроить в backend для production

### 7.4 Сборка и загрузка в App Store

**Команды:**

```bash
# Сборка production build
npx expo prebuild --platform ios --clean
cd ios
# Открыть в Xcode
xed .
```

**В Xcode:**
1. Product → Archive
2. Distribute App → App Store Connect
3. Upload

**После загрузки:**
1. Зайти в App Store Connect
2. Создать новую версию
3. Заполнить все метаданные
4. Прикрепить build
5. Submit for Review

### 7.5 App Review Guidelines

**Важно проверить:**
- Работает ли приложение без краш��в
- Privacy Policy доступна
- Push уведомления можно отключить
- Нет запрещенного контента
- Все функции работают

**Время ревью:** обычно 24-48 часов

## Этап 8: Веб версия (после релиза мобильных приложений)

### 8.1 Настройка Expo для Web

**Команды:**

```bash
npx expo install react-dom react-native-web @expo/metro-runtime
npx expo export:web
```

### 8.2 Адаптация UI для Web

**Файлы для создания/обновления:**

- `app.json` - добавить web конфигурацию
- Добавить media queries для desktop разрешений
- Создать отдельные стили для desktop в каждом screen

**Особенности:**
- Mobile web: идентичен iOS (responsive до 768px)
- Desktop web: другой layout (sidebar навигация, grid для контента)

### 8.3 Responsive стили для Desktop

**Компоненты требующие адаптации:**

- Навигация: Sidebar вместо bottom tabs
- Dashboard: Grid 2-3 колонки
- Форум: Desktop layout с боковой панелью категорий
- Калькуляторы: Side-by-side layout

**Утилита:** использовать `useMediaQuery` или `Dimensions`

### 8.4 Деплой веб версии

**Варианты хостинга:**
- **Vercel:** проще всего для Expo web
- **Netlify:** альтернатива
- **AWS S3 + CloudFront:** для полного контроля

**Файлы создать:**
- `vercel.json` - конфигурация для Vercel
- Настроить custom domain

### 8.5 SEO оптимизация

**Мета-теги:**
- Title, description для каждой страницы
- Open Graph теги
- Twitter Card

**Файл:** `app/+html.tsx` - обновить с SEO тегами

### 8.6 PWA функциональность

- Service Worker для offline режима
- Web App Manifest
- Добавить на главный экран

## Этап 9: Финальное тестирование и релиз

### 9.1 Полное тестирование всех платформ

**Тестирование:**
- iOS TestFlight (внутреннее тестирование)
- Android Internal Testing Track
- Web (staging environment)

**Чек-лист функциональности:**
- [ ] Авторизация (регистрация, логин, восстановление пароля)
- [ ] Календарь отчетов
- [ ] Новости (загрузка, фильтрация, детали)
- [ ] Все 7 калькуляторов
- [ ] Поиск документов (Google API)
- [ ] Форум (создание топиков, комментирование, лайки)
- [ ] Профиль (редактирование, настройки)
- [ ] Push уведомления (дедлайны, персонализированные новости)
- [ ] Производительность (нет лагов, быстрая загрузка)

### 9.2 Мониторинг и аналитика

**Инструменты:**
- **Sentry:** для отслеживания ошибок
- **Google Analytics:** для аналитики использования
- **Firebase Crashlytics:** для краш-репортов

**Файлы создать:**
- Интеграция Sentry в frontend и backend
- Настроить события аналитики для ключевых действий

### 9.3 Подготовка к релизу

**Backend:**
- [ ] Все миграции применены на production
- [ ] Celery tasks запущены и работают
- [ ] Краулеры собирают новости
- [ ] Push уведомления настроены
- [ ] Health checks работают
- [ ] Бэкапы БД настроены

**Frontend:**
- [ ] Production builds собраны без ошибок
- [ ] API URL указывает на production
- [ ] Все секреты в secure storage
- [ ] App версии обновлены (1.0.0)

### 9.4 Release Checklist

**iOS App Store:**
- [ ] Build загружен в App Store Connect
- [ ] Метаданные заполнены
- [ ] Скриншоты загружены
- [ ] Privacy Policy добавлена
- [ ] Submit for Review нажато

**Google Play:**
- [ ] AAB загружен в Play Console
- [ ] Store listing заполнен
- [ ] Скриншоты загружены
- [ ] Privacy Policy добавлена
- [ ] Production release создан

**Web:**
- [ ] Деплой на production (Vercel/S3)
- [ ] Custom domain настроен
- [ ] SSL сертификат активен
- [ ] SEO теги проверены

### 9.5 Пост-релизный план

**Мониторинг первой недели:**
- Отслеживать краш-репорты
- Собирать отзывы пользователей
- Исправлять критические баги оперативно
- Следить за серверными ресурсами (CPU, RAM, DB)

**Обновления:**
- Hotfix релизы при критических багах
- Минорные обновления раз в 2 недели
- Мажорные обновления раз в 1-2 месяца

## Приоритеты и последовательность

### Фаза 1: Базовые улучшения (1-2 дня)
1. ✅ UI/UX улучшения (клавиатура, цвета)
2. Tax.gov.ua парсер на Playwright

### Фаза 2: Форум (3-5 дней)
3. Backend форума (модели, API)
4. Frontend форума (экраны, компоненты)
5. Тестирование форума

### Фаза 3: Дополнительные парсеры (1-2 дня)
6. Dtkt.ua парсер
7. Опционально: zakon.rada.gov.ua и diia.gov.ua

### Фаза 4: Деплой backend (2-3 дня)
8. Настройка AWS инфраструктуры
9. Деплой на EC2
10. Тестирование production backend

### Фаза 5: Android релиз (2-3 дня)
11. Сборка и тестирование на Android
12. Исправление Android-специфичных багов
13. Подготовка материалов для Play Store
14. Публикация в Google Play

### Фаза 6: iOS релиз (2-3 дня)
15. Ожидание активации Apple Developer Account
16. Сборка и тестирование на iOS
17. Подготовка материалов для App Store
18. Публикация в App Store

### Фаза 7: Веб версия (3-4 дня)
19. Адаптация для Web
20. Desktop-specific UI
21. Деплой на Vercel
22. SEO и PWA

### Фаза 8: Финальное тестирование (1-2 дня)
23. Полное тестирование всех платформ
24. Исправление найденных багов
25. Релиз!

## Итого: 15-24 рабочих дня до полного релиза

---

**Последнее обновление:** 21 ноября 2024

