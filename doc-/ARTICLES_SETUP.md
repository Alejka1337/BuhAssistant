# Статті від авторів - Документація

**Дата создания:** 6 декабря 2025  
**Последнее обновление:** 7 декабря 2025  
**Статус:** ✅ Полностью реализовано

---

## Обзор

Реализован полнофункциональный раздел со статьями от модераторов и администраторов. Статьи отображаются **только на веб-версии** приложения.

### ✨ Обновления от 7 декабря 2025

1. ✅ **Модальное окно успеха** при создании/редактировании статьи
2. ✅ **Редактирование статей** (автор, модераторы, админы)
3. ✅ **Удаление статей** (только админы)
4. ✅ **SEO мета-теги** (title, description) для индексации в поисковых системах

---

## Что реализовано

### Backend ✅

1. **Модель `Article`** (`backend/app/models/article.py`)
   - Поля: id, title, slug, content (HTML), excerpt, cover_image
   - SEO: meta_title, meta_description (добавлено 7.12.2025)
   - Автор (author_id), статус публикации (is_published)
   - Счетчик просмотров, временные метки

2. **API Endpoints** (`backend/app/api/articles.py`)
   - `GET /api/articles` - список статей с пагинацией и поиском
   - `GET /api/articles/{slug}` - одна статья по slug
   - `POST /api/articles` - создание (модераторы/админы)
   - `PUT /api/articles/{id}` - редактирование (автор, модераторы, админы)
   - `DELETE /api/articles/{id}` - удаление (только админы)

3. **Pydantic схемы** (`backend/app/schemas/article.py`)
   - ArticleCreate, ArticleUpdate, ArticleResponse
   - ArticleListItem, ArticleListResponse

4. **Автоматическая генерация slug**
   - Транслитерация украинского в латиницу
   - Уникальность (добавляется счетчик при дубликатах)

5. **Alembic миграция** - таблица `articles` создана

### Frontend (Web only) ✅

1. **Сервис** (`utils/articleService.ts`)
   - Функции для работы с API (CRUD)
   - Типы TypeScript (обновлены с SEO полями)

2. **Страница списка статей** (`app/articles.web.tsx`)
   - Grid layout с карточками статей
   - Поиск по заголовкам
   - Пагинация
   - Кнопка создания (для модераторов/админов)
   - Responsive design (desktop + mobile web)

3. **Страница статьи** (`app/article/[slug].web.tsx`)
   - Детальный просмотр статьи
   - Обложка, метаинформация
   - HTML контент с правильным рендерингом
   - Счетчик просмотров
   - **НОВОЕ:** Кнопки редактирования и удаления (для авторов/админов)
   - **НОВОЕ:** SEO мета-теги (title, description) для Google/Bing
   - **НОВОЕ:** Модальное окно подтверждения удаления

4. **Страница редактора** (`app/admin/articles/new.web.tsx`)
   - Создание и редактирование статей
   - HTML редактор с toolbar
   - Preview режим
   - **НОВОЕ:** SEO поля (meta_title, meta_description)
   - Переключатель публикации
   - **НОВОЕ:** Модальное окно успеха с автоматическим редиректом
   - Доступ только для модераторов/админов
   - Поддержка редактирования через `/admin/articles/edit?slug=...`

5. **Компоненты**
   - **НОВОЕ:** `SuccessModal.tsx` - модальное окно успеха с иконкой
   - `ConfirmModal.tsx` - переиспользуется для подтверждения удаления

5. **Навигация**
   - Добавлена в Sidebar (desktop)
   - Добавлена в MobileMenu (mobile web)
   - Иконка: library-books

---

## Права доступа

### Просмотр статей
- **Все пользователи** (включая гостей) - могут видеть **опубликованные** статьи

### Создание статей
- **Модераторы** - могут создавать статьи
- **Админы** - могут создавать статьи

### Редактирование статей
- **Автор** (модератор) - может редактировать **свои** статьи
- **Админы** - могут редактировать **любые** статьи

### Удаление статей
- **Только админы** - могут удалять статьи

---

## Использование

### Создание статьи

1. Войти как модератор/админ
2. Перейти на страницу "Статті" в меню
3. Нажать "Створити статтю"
4. Заполнить поля:
   - **Заголовок** (обязательно, 5-255 символов)
   - **Короткий опис** (опционально, до 500 символов)
   - **URL обкладинки** (опционально)
   - **Зміст** (обязательно, HTML, минимум 50 символов)
5. Выбрать статус публикации (опубліковано/чернетка)
6. Нажать "Створити"

### HTML редактор

Редактор поддерживает базовые HTML теги через toolbar:
- **H2**, **H3** - заголовки
- **P** - параграфы
- **B** (strong) - жирный текст
- **I** (em) - курсив
- **UL** - ненумерованный список
- **LI** - элемент списка

Можно вставлять HTML вручную:
- Ссылки: `<a href="URL">текст</a>`
- Изображения: `<img src="URL" alt="описание">`
- Цитаты: `<blockquote>текст</blockquote>`
- Код: `<code>код</code>`

### Режим предпросмотра

Нажмите кнопку "Попередній перегляд" для просмотра статьи с HTML рендерингом.

---

## Структура файлов

```
backend/
├── app/
│   ├── models/
│   │   └── article.py                 # Модель Article
│   ├── schemas/
│   │   └── article.py                 # Pydantic схемы
│   ├── api/
│   │   └── articles.py                # API endpoints
│   └── main.py                         # Подключение роутера

frontend/
├── utils/
│   └── articleService.ts               # Сервис для работы с API
├── app/
│   ├── articles.tsx                    # Базовый файл (redirect на .web)
│   ├── articles.web.tsx                # Список статей (Web)
│   ├── article/
│   │   ├── [slug].tsx                  # Базовый файл
│   │   └── [slug].web.tsx              # Детальный просмотр (Web)
│   └── admin/
│       └── articles/
│           ├── new.tsx                 # Базовый файл
│           └── new.web.tsx             # Редактор (Web)
└── components/
    └── web/
        ├── Sidebar.tsx                 # Обновлен (добавлена навигация)
        └── MobileMenu.tsx              # Обновлен (добавлена навигация)
```

---

## API Endpoints

### GET `/api/articles`
Получить список статей с пагинацией

**Query Parameters:**
- `page` (int, default: 1) - номер страницы
- `per_page` (int, default: 20) - количество статей на странице
- `search` (string, optional) - поиск по заголовку
- `published_only` (bool, default: true) - показывать только опубликованные

**Response:**
```json
{
  "articles": [...],
  "total": 42,
  "page": 1,
  "per_page": 20,
  "total_pages": 3
}
```

### GET `/api/articles/{slug}`
Получить статью по slug

**Response:**
```json
{
  "id": 1,
  "title": "Заголовок",
  "slug": "zagolovok",
  "content": "<p>HTML контент</p>",
  "excerpt": "Короткое описание",
  "cover_image": "https://...",
  "author_id": 1,
  "author": {
    "id": 1,
    "full_name": "Иван Иванов",
    "email": "ivan@example.com"
  },
  "is_published": true,
  "views": 123,
  "created_at": "2025-12-06T...",
  "updated_at": "2025-12-06T...",
  "published_at": "2025-12-06T..."
}
```

### POST `/api/articles`
Создать статью (требует авторизации, роль: модератор/админ)

**Request Body:**
```json
{
  "title": "Заголовок",
  "content": "<p>HTML контент</p>",
  "excerpt": "Краткое описание",
  "cover_image": "https://...",
  "is_published": true
}
```

### PUT `/api/articles/{id}`
Обновить статью (требует авторизации и прав доступа)

**Request Body:** (все поля опциональны)
```json
{
  "title": "Новый заголовок",
  "content": "<p>Обновленный контент</p>",
  "is_published": true
}
```

### DELETE `/api/articles/{id}`
Удалить статью (требует роли админа)

---

## Тестирование

### 1. Создание тестовой статьи

```bash
# Войти как пользователь с ролью MODERATOR или ADMIN
# Перейти на /admin/articles/new
```

### 2. Проверка прав доступа

```python
# Обновить роль пользователя в БД (если нужно)
docker-compose exec postgres psql -U eglavbuh_user -d eglavbuh_db

UPDATE users SET role = 'MODERATOR' WHERE email = 'test@example.com';
# или
UPDATE users SET role = 'ADMIN' WHERE email = 'admin@example.com';
```

### 3. Проверка в БД

```sql
-- Посмотреть все статьи
SELECT id, title, slug, is_published, views, author_id, created_at 
FROM articles 
ORDER BY created_at DESC;

-- Посмотреть опубликованные статьи
SELECT * FROM articles WHERE is_published = true;
```

---

## Дополнительные возможности (для будущих доработок)

1. **Rich text редактор**
   - Установить React Quill или TipTap
   - Заменить textarea на полноценный WYSIWYG редактор

2. **Загрузка изображений**
   - Добавить файловое хранилище (S3, Cloudinary)
   - Endpoint для загрузки обложек

3. **Категории статей**
   - Модель ArticleCategory
   - Фильтрация по категориям

4. **Комментарии к статьям**
   - Модель ArticleComment
   - API для комментариев

5. **Рейтинг статей**
   - Модель ArticleRating
   - Лайки/дизлайки

6. **SEO оптимизация**
   - Meta tags (description, keywords)
   - Open Graph tags
   - Sitemap

---

## 📝 Инструкции по использованию новых функций

### 1. Создание статьи с SEO

1. Перейдите в раздел **"Статті"** → нажмите **"Створити статтю"**
2. Заполните основные поля:
   - **Заголовок** (обязательно, 5-255 символов)
   - **Короткий опис** (опционально, для списка статей)
3. В разделе **🔍 SEO налаштування**:
   - **SEO Title**: Альтернативный заголовок для Google (если не указан, используется основной заголовок)
   - **SEO Description**: Описание для поисковых систем (рекомендуется 150-160 символов)
4. Заполните **URL обложки** и основной **контент** (HTML редактор)
5. Включите **"Опублікувати відразу"** если хотите опубликовать немедленно
6. Нажмите **"Створити статтю"**

**Результат:** Появится модальное окно успеха с кнопкой **"Переглянути статтю"**, которая перенаправит вас на созданную статью.

### 2. Редактирование статьи

**Права доступа:**
- Админ может редактировать **любые** статьи
- Модератор может редактировать **только свои** статьи

**Как редактировать:**
1. Откройте статью для просмотра
2. В правом верхнем углу (под заголовком) появятся кнопки:
   - **Редагувати** (если у вас есть права)
   - **Видалити** (только для админов)
3. Нажмите **"Редагувати"**
4. Внесите изменения (включая SEO поля)
5. Нажмите **"Оновити статтю"**

**Результат:** Появится модальное окно успеха с сообщением **"Статтю успішно оновлено"**.

### 3. Удаление статьи

**Права доступа:** Только **ADMIN**

**Как удалить:**
1. Откройте статью для просмотра
2. Нажмите кнопку **"Видалити"** (красная, с иконкой корзины)
3. Подтвердите удаление в модальном окне
4. Нажмите **"Видалити"**

**Результат:** Статья удаляется из БД, вы перенаправляетесь на список статей.

⚠️ **Внимание:** Удаление необратимо!

### 4. SEO в поисковых системах

**Как работает:**
- При открытии статьи автоматически устанавливаются мета-теги:
  - `<title>` = `meta_title` или `title` (если meta_title не указан)
  - `<meta name="description">` = `meta_description` или `excerpt` или `title`

**Проверка индексации:**
1. Откройте статью в браузере
2. Нажмите **F12** (Developer Tools)
3. Перейдите в **Elements** → найдите `<head>`
4. Проверьте наличие:
   ```html
   <title>Ваш SEO заголовок</title>
   <meta name="description" content="Ваш SEO описание">
   ```

**Рекомендации:**
- **SEO Title:** 50-60 символов (включая бренд: "Заголовок | eGlavBuh")
- **SEO Description:** 150-160 символов, включайте ключевые слова
- Используйте уникальные title/description для каждой статьи

---

## Troubleshooting

### Проблема: "У вас немає прав для створення статей"

**Решение:** Проверить роль пользователя в БД:
```sql
SELECT id, email, role FROM users WHERE email = 'your@email.com';
```
Обновить роль на MODERATOR или ADMIN.

### Проблема: Статья не отображается в списке

**Решение:** Проверить статус публикации:
```sql
SELECT id, title, is_published FROM articles;
```
Убедиться, что `is_published = true` и `published_at` не NULL.

### Проблема: HTML не рендерится правильно

**Решение:** 
1. Проверить HTML в режиме предпросмотра
2. Убедиться, что используются правильные теги
3. Проверить закрытие всех тегов

---

**Статус:** ✅ Этап 6 завершен! Статьи готовы к использованию.

