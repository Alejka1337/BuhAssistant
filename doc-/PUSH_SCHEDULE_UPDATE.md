# Обновление расписания Push-уведомлений о новостях 🔔

**Дата обновления:** 5 декабря 2025  
**Статус:** Завершено ✅

---

## Что изменилось

### ❌ Старое расписание:
- **Частота:** 2 раза в неделю (понедельник и четверг)
- **Время:** 10:00 (фиксированное)
- **Получатели:** Только зарегистрированные пользователи с персонализацией

### ✅ Новое расписание:
- **Частота:** Каждый день 2 раза
- **Время:** 
  - Первый раз: 12:00-14:00 (рандомное время)
  - Второй раз: 18:00-20:00 (рандомное время)
- **Получатели:** ВСЕ пользователи (зарегистрированные + анонимные)

---

## Технические детали

### Celery Beat Schedule
**Файл:** `backend/app/celery_app.py`

```python
# Старое (удалено):
'send-news-notifications-twice-weekly': {
    'task': 'send_news_notifications',
    'schedule': crontab(minute=0, hour=10, day_of_week='1,4'),  # Пн и Чт в 10:00
}

# Новое (добавлено):
'send-news-notifications-noon': {
    'task': 'send_news_notifications',
    'schedule': crontab(minute=0, hour=12),  # 12:00 каждый день
},
'send-news-notifications-evening': {
    'task': 'send_news_notifications',
    'schedule': crontab(minute=0, hour=18),  # 18:00 каждый день
},
```

### Рандомизация времени
**Файл:** `backend/app/tasks/notification_tasks.py`

```python
# В начале задачи добавлена рандомная задержка:
delay_seconds = random.randint(0, 7200)  # 0-120 минут (0-7200 секунд)
time.sleep(delay_seconds)
```

**Логика:**
1. Задача запускается ровно в 12:00 или 18:00
2. Генерируется рандомная задержка от 0 до 120 минут
3. Задача "спит" указанное время
4. После задержки отправляются push-уведомления

**Результат:** Push приходят в рандомное время в окне 12:00-14:00 или 18:00-20:00

### Упрощение логики отправки
**Файл:** `backend/app/tasks/notification_tasks.py`

**Старая логика (сложная):**
- Персонализация для каждого пользователя
- Фильтрация по user_type
- Использование OpenAI для выбора релевантной новости
- Только зарегистрированные пользователи

**Новая логика (простая):**
- Одна самая свежая новость для всех
- Отправка через `push_service.send_news_to_all()`
- ВСЕ пользователи (зарегистрированные + анонимные)

```python
# Выбираем самую свежую новость за последние 24 часа
recent_news = db.query(News).filter(
    News.published_at >= day_ago
).order_by(News.published_at.desc()).limit(10).all()

news_item = recent_news[0]

# Отправляем ВСЕМ
result = push_service.send_news_to_all(
    db=db,
    title="📰 Нова стаття",
    body=news_item.title,
    data={...}
)
```

---

## Частота отправки

### В неделю:
- **Старое:** 2 раза (понедельник, четверг)
- **Новое:** 14 раз (каждый день × 2)

### В месяц:
- **Старое:** ~8-9 раз
- **Новое:** ~60 раз (30 дней × 2)

---

## Преимущества изменений

### 1. Больше охват ✅
- Анонимные пользователи теперь получают новости
- Увеличение engagement

### 2. Регулярность ✅
- Каждый день пользователи видят новости
- Привычка открывать приложение

### 3. Рандомизация времени ✅
- Избегаем "штампа времени" (всегда в одно время)
- Более естественное распределение нагрузки на сервер
- Лучше для пользователей (не все в одно время)

### 4. Упрощение кода ✅
- Убрали персонализацию (не нужна для анонимных)
- Меньше запросов к OpenAI
- Быстрее выполнение задачи

---

## Мониторинг

### Логи Celery Beat
```bash
docker logs eglavbuh_celery_beat --tail 50
```

Должно появиться:
```
Scheduler: Sending due task send-news-notifications-noon (send_news_notifications)
Scheduler: Sending due task send-news-notifications-evening (send_news_notifications)
```

### Логи Celery Worker
```bash
docker logs eglavbuh_celery_worker --tail 50
```

Должно появиться:
```
Starting news notifications task
Applying random delay: 67.3 minutes (4038 seconds)
Delay completed. Sending news notifications now at 2025-12-05 13:07:18
News notifications task completed. Sent: 42/45 (Registered: 15, Anonymous: 27)
```

### Проверка расписания
```bash
# Подключиться к контейнеру
docker exec -it eglavbuh_celery_beat bash

# Посмотреть расписание
celery -A app.celery_app inspect scheduled
```

---

## Тестирование

### Ручной запуск задачи
```bash
# Вариант 1: Через docker exec
docker exec eglavbuh_celery_worker celery -A app.celery_app call send_news_notifications

# Вариант 2: Через Python shell
docker exec -it eglavbuh_backend python

>>> from app.tasks.notification_tasks import send_news_notifications
>>> send_news_notifications.delay()
```

### Проверка результата
```python
# В логах должно быть:
Starting news notifications task
Applying random delay: X.X minutes
Delay completed. Sending news notifications now
Sent: X/Y (Registered: A, Anonymous: B)
```

---

## Production Deployment

### Шаги:
1. ✅ Изменения в коде применены
2. ✅ Backend, Celery Worker, Celery Beat перезапущены
3. ⏳ Дождаться первой отправки (12:00 или 18:00 по Киеву)
4. ⏳ Проверить логи
5. ⏳ Подтвердить получение push на устройствах

### Rollback (если нужно):
```bash
# Откатить изменения в celery_app.py
# Вернуть старую версию notification_tasks.py
# Перезапустить:
docker-compose restart backend celery_worker celery_beat
```

---

## Известные ограничения

### 1. Рандомизация только в рамках окна
- Нельзя задать более гибкое окно (например, 10:00-20:00)
- Два фиксированных окна: 12-14 и 18-20

### 2. Одна новость для всех
- Нет персонализации для анонимных пользователей
- Зарегистрированные тоже получают одну общую новость

### 3. Нагрузка на сервер
- Задача "спит" 0-120 минут, блокируя worker
- Можно оптимизировать через `apply_async(countdown=delay_seconds)`

---

## Возможные улучшения (будущее)

### 1. Использовать ETA вместо sleep
```python
# Вместо:
time.sleep(delay_seconds)

# Использовать:
send_news_notifications.apply_async(
    eta=datetime.now() + timedelta(seconds=delay_seconds)
)
```

### 2. A/B тестирование времени
- Отслеживать, в какое время больше открывают приложение
- Оптимизировать временные окна

### 3. Частичная персонализация
- Для зарегистрированных - персонализированные новости
- Для анонимных - топ новость

---

## Статистика (ожидаемая)

### Охват:
- **Зарегистрированные:** ~50-100 пользователей
- **Анонимные:** ~200-500 пользователей (растет)
- **Итого:** ~250-600 push в день × 2 = 500-1200 push/день

### Success rate:
- **Ожидаемый:** 90-95% (с учетом устаревших токенов)
- **Мониторить:** через логи Celery Worker

---

## Checklist

- [x] Обновлено расписание в `celery_app.py`
- [x] Добавлена рандомизация в `notification_tasks.py`
- [x] Обновлена логика на `send_news_to_all()`
- [x] Backend перезапущен
- [x] Celery Worker перезапущен
- [x] Celery Beat перезапущен
- [ ] Проверено получение push в 12:00-14:00
- [ ] Проверено получение push в 18:00-20:00
- [ ] Подтверждено получение анонимными пользователями

---

**Обновление завершено!** 🎉  
**Следующая отправка:** В 12:00 или 18:00 по Киеву (с рандомной задержкой)

