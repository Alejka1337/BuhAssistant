# BuhAssistant MVP Development Plan

## ✅ Етап 1: Доопрацювання існуючого функціоналу (ЗАВЕРШЕНО)

**Мета:** Завершити базові фічі, що вже частково реалізовані, зробити їх production-ready.

### Frontend:

- Доробити всі калькулятори (перевірити формули, валідацію, UI)
- Покращити AccountingCalendar: додати можливість завантаження JSON файлів динамічно, обробку помилок
- Інтегрувати всі калькулятори в екран Tools (`app/(tabs)/tools.tsx`)
- Перевірити форму ConsultationModal на коректність відправки
- Додати базову обробку помилок та loading states

### Тестування:

- Перевірити всі калькулятори з реальними даними
- Протестувати форму консультації (відправка email)
- Перевірити календар з різними місяцями

---

## ✅ Етап 2: Backend інфраструктура (Foundation) - ЗАВЕРШЕНО

**Мета:** Підняти базову backend інфраструктуру для подальшої розробки.

### Backend Setup:

- Створити FastAPI проект структуру (`/backend` папка)
- Налаштувати Docker Compose з сервісами:
  - PostgreSQL
  - Redis
  - Elasticsearch
  - FastAPI app
- Налаштувати SQLAlchemy + Alembic для міграцій
- Створити базові моделі БД (users, news, forum_threads, forum_posts)
- Налаштувати CORS для React Native
- Створити health check endpoints

### CI/CD:

- Налаштувати `.gitlab-ci.yml` для автодеплою на EC2
- Dockerfile для production
- Базовий deployment скрипт

### Тестування:

- Перевірити доступність всіх сервісів через Docker
- Протестувати підключення до БД
- Перевірити health endpoints

---

## ✅ Етап 3: Пошук (Search Feature) - ЗАВЕРШЕНО

**Мета:** Реалізувати пошук по авторитетних джерелах з можливістю фільтрації.

### Frontend: ✅ ЗАВЕРШЕНО

- ✅ Створити UI для пошуку в `app/(tabs)/search.tsx`
- ✅ Додати фільтри по джерелах (multi-select modal з чекбоксами)
- ✅ Створити SearchResultCard компонент для відображення результатів
- ✅ Реалізувати `utils/searchService.ts` з mock даними
- ✅ Додати loading states, error handling, placeholder
- ✅ Інтегрувати відкриття статей через Linking.openURL
- ⏳ Інтегрувати з Google Custom Search API або SerpAPI (наступний крок)

**Поточний стан:**
- Пошук працює з mock даними для тестування UI
- Логіка формування запросів `site:domain.com` реалізована
- UI повністю готовий для production
- Див. документацію: `docs/GOOGLE_SEARCH_INTEGRATION.md`

### Backend: ✅ ЗАВЕРШЕНО

- ✅ Створити endpoint `/api/search/` для проксування запитів до пошукового API
- ✅ Додати кешування результатів у Redis (TTL 1 година)
- ✅ Логування пошукових запитів у БД для аналітики (таблиця search_logs)
- ✅ Реалізовано Google парсинг через BeautifulSoup4 (aiohttp + паралельні запити)
  - ⚠️ Google блокує прямий парсинг HTML
  - 📝 Див. `backend/docs/GOOGLE_PARSING_ISSUE.md` для альтернатив
- ✅ **Google Custom Search API ІНТЕГРОВАНО** 🎉
  - API Key та Search Engine ID налаштовані
  - Автоматичне переключення між API та mock даними
  - Локалізація: gl=ua (Україна), hl=uk (українська мова)
  - Витяг даних: title, snippet, url, date з metatags
  - Ліміт: 100 безкоштовних запитів/день
  - Вартість: $5 за 1000 додаткових запитів
- ✅ Створено Pydantic схеми з enum для sources
  - `SourceEnum` - всі доступні домени
  - `SearchRequest` - приймає домени напряму
  - `SearchResponse` - з полем `cached` та `date`
  - `SearchResult` - з підтримкою дати останнього оновлення
- ✅ Endpoint `/api/search/stats` для аналітики популярних запитів
- ✅ Правильні селектори для Google парсингу (div.MjjYud, h3.LC20lb, div.VwiC3b)
- ✅ Міграція БД для search_logs виконана
- ✅ Frontend інтегрований з backend API (домени замість ID)

### Тестування: ✅ ГОТОВО ДО ТЕСТУВАННЯ

- ✅ Перевірити пошук по кожному джерелу окремо (UI готовий)
- ✅ Протестувати фільтри (multi-select працює)
- ✅ Перевірити відкриття статей через браузер
- ✅ Перевірити роботу без інтернету (обробка помилок додана)

---

## Етап 4: Краулер новин (News Crawler System) ✅ (ЧАСТКОВО ЗАВЕРШЕНО)

**Мета:** Реалізувати систему збору та аналізу новин з авторитетних джерел.

**Статус**: ✅ **ПОВНІСТЮ ЗАВЕРШЕНО**
- ✅ Реалізовано краулер для minfin.com.ua (працює: 200 спарсено → 4 відфільтровано, 2%)
- ✅ Реалізовано краулер для liga.net (працює: 55 спарсено → 5 відфільтровано, 9%)
- ✅ Реалізовано краулер для buhgalter911.com (працює: 30 спарсено → 17 відфільтровано, 57%!)
- ✅ Інтегровано OpenAI API для фільтрації та категоризації новин
- ✅ Налаштовано Celery + Redis для автоматичного парсингу **2 рази на день** (8:00, 20:00)
- ✅ Створено API endpoints для роботи з новинами
- ✅ Відображення останніх 3 новин на головному екрані
- ✅ Перевірка дублікатів по URL перед збереженням
- ⚠️ Краулер для tax.gov.ua створено, але заблоковано CDN (потрібен Playwright для production)
- ⏳ Можна додати: zakon.rada.gov.ua (опційно)

### Backend:

- Створити папку `/backend/crawlers` з окремими scrapers для кожного сайту:
  - `tax_gov_ua_crawler.py`
  - `zakon_rada_crawler.py`
  - `minfin_crawler.py`
  - `liga_net_crawler.py`
  - `buhgalter911_crawler.py`
- Налаштувати Celery + Redis для периодичних tasks
- Створити Celery task для запуску краулерів кожні 2-3 години
- Інтегрувати OpenAI API для аналізу аудиторії (NER + categorization)
- Створити endpoints:
  - `GET /api/news` - список новин з пагінацією
  - `GET /api/news/{id}` - деталі новини
- Налаштувати Elasticsearch індекси для новин
- Створити систему тегування (ФОП 1/2/3, юрособи, ЄСВ, ПДВ тощо)

### Frontend:

- Створити екран з новинами (Home або окремий таб)
- Відображення новин у форматі стрічки
- Фільтри за категоріями та датами
- Pull-to-refresh для оновлення
- Детальний перегляд новини у WebView

### Тестування:

- Запустити краулери вручну, перевірити збір даних
- Перевірити коректність категоризації через OpenAI
- Протестувати відображення новин у застосунку
- Перевірити фільтри та пагінацію

---

## ✅ Етап 5: API для календаря (Calendar API Migration) - ЗАВЕРШЕНО

**Мета:** Перенести календарь отчетов на backend для упрощения обновления дат и событий без необходимости пересборки приложения.

**Статус**: ✅ **ПОВНІСТЮ ЗАВЕРШЕНО**

### Backend: ✅ ЗАВЕРШЕНО

- ✅ Створено директорію `/backend/data/calendar/` для зберігання JSON файлів
- ✅ Створено Pydantic схеми (`backend/app/schemas/calendar.py`):
  - `CalendarEvent` - одна подія календаря
  - `CalendarResponse` - відповідь з подіями для місяця
  - `AvailablePeriod` - доступний період
  - `AvailablePeriodsResponse` - список доступних періодів
- ✅ Створено сервісний шар (`backend/app/services/calendar_service.py`):
  - `get_calendar_events(month, year)` - завантаження подій з JSON
  - `get_available_periods()` - сканування доступних місяців
  - `validate_calendar_data()` - валідація структури даних
- ✅ Створено API endpoints (`backend/app/api/calendar.py`):
  - `GET /api/calendar/?month=10&year=2025` - отримання календаря
  - `GET /api/calendar/available-months` - список доступних періодів
  - `GET /api/calendar/health` - health check
- ✅ Додано calendar router у `main.py`
- ✅ Скопійовано JSON файли з `/data/` у `/backend/data/calendar/`

### Frontend: ✅ ЗАВЕРШЕНО

- ✅ Створено `utils/calendarService.ts`:
  - `fetchCalendarEvents(month, year)` - завантаження подій
  - `fetchAvailablePeriods()` - отримання доступних періодів
  - `fetchMultipleMonths(periods)` - паралельне завантаження кількох місяців
- ✅ Оновлено `constants/api.ts` - додано `CALENDAR` endpoint
- ✅ Рефакторинг `components/AccountingCalendar.tsx`:
  - Замінено локальні JSON imports на API calls
  - Реалізовано async завантаження поточного та наступного місяця
  - Додано обробку помилок та empty states
  - Loading states вже були реалізовані раніше

### Тестування: ✅ ПРОТЕСТОВАНО

- ✅ Перевірено API endpoint `/api/calendar/?month=10&year=2025` (16 подій)
- ✅ Перевірено `/api/calendar/available-months` (2 періоди: 10/2025, 11/2025)
- ✅ Calendar health check працює
- ✅ Frontend компонент готовий до тестування з backend

### Переваги:

1. **Централізоване управління**: оновлення календаря в одному місці
2. **Без пересборки**: зміни доступні одразу після оновлення JSON на сервері
3. **Масштабованість**: легко додавати нові місяці/роки
4. **Сумісність**: структура даних залишилась незмінною
5. **Простота**: мінімальні зміни в існуючому коді

**Примітка**: Локальні JSON файли в `/data/` можна видалити після успішного тестування frontend з backend API.

---

## Етап 6: Авторизація (Authentication) ✅ ЗАВЕРШЕНО

**Мета:** Реалізувати систему реєстрації та авторизації з email+password та Google OAuth2.

**Статус:** ✅ **Email/Password ПОВНІСТЮ ГОТОВО** | ⏸️ **Google OAuth2 ВІДКЛАДЕНО**

**Примітка по Google OAuth2:**
- Google Web Client ID не приймає custom URI schemes (`com.googleusercontent.apps.*`)
- Для iOS потрібна інтеграція з Firebase Auth або використання WebView flow
- На даний момент Google Sign In **відключено** (закоментовано в `app/login.tsx`)
- TODO: Розглянути варіанти:
  1. Інтеграція з Firebase Authentication
  2. Використання WebView для OAuth flow
  3. Використання Universal Links (потребує Apple Developer Account та domain)

### Backend: ✅ ЗАВЕРШЕНО

- ✅ Налаштовано JWT authentication (access + refresh tokens)
- ✅ Створено endpoints:
  - ✅ `POST /api/auth/register` - реєстрація
  - ✅ `POST /api/auth/login` - логін
  - ✅ `POST /api/auth/refresh` - оновлення токена
  - ✅ `POST /api/auth/verify-email` - підтвердження email
  - ✅ `POST /api/auth/resend-verification` - повторна відправка коду
  - ⚠️ `POST /api/auth/google` - Google OAuth2 callback (ПОТРЕБУЄ ПЕРЕВІРКИ)
- ✅ Налаштовано Google OAuth2 (credentials, redirect URIs)
- ✅ Створено middleware для перевірки JWT токенів
- ✅ Додано email verification з 6-значним кодом
- ✅ Email сервіс для відправки кодів верифікації

### Frontend: ✅ ЗАВЕРШЕНО

- ✅ Встановлено необхідні пакети (expo-auth-session, expo-secure-store)
- ✅ Створено екрани:
  - ✅ `app/login.tsx` - Login screen
  - ✅ `app/register.tsx` - Register screen
  - ✅ `app/verify-email.tsx` - Email verification screen
- ⚠️ Налаштовано Google Sign In через expo-auth-session (ПОТРЕБУЄ ВИПРАВЛЕННЯ)
- ✅ Створено `AuthContext` для зберігання auth state
- ✅ Зберігання токенів у SecureStore (expo-secure-store)
- ✅ Додано Protected Routes для авторизованих користувачів
- ✅ Обробка неверифікованих користувачів при логіні

### Тестування: ✅ ПРОТЕСТОВАНО

- ✅ Протестовано реєстрацію з email+password
- ✅ Перевірено логін
- ✅ Протестовано email verification flow
- ⚠️ Google OAuth2 flow ПОТРЕБУЄ ВИПРАВЛЕННЯ
- ✅ Перевірено refresh token механізм
- ✅ Протестовано logout

### Відомі проблеми:

- ⚠️ Google OAuth2: попередня реалізація не працювала (див. `doc-files/GOOGLE_OAUTH_*.md`)
- 📝 Потрібно переглянути та виправити Google OAuth flow

---

## Етап 7: Профіль та персоналізація (Profile & Personalization) ✅ ЗАВЕРШЕНО

**Мета:** Створити особистий кабінет користувача з можливістю налаштування профілю.

**Статус:** ✅ **ПОВНІСТЮ РЕАЛІЗОВАНО**

### Backend: ✅ ЗАВЕРШЕНО

- ✅ Розширено User модель полями:
  - ✅ `user_type` (ФОП/Юрособа/Бухгалтер/Фізособа) - Enum з валідацією
  - ✅ `fop_group` (1/2/3) - Enum для ФОП груп
  - ✅ `tax_system` (для юросіб) - String поле
  - ✅ `notification_preferences` - окрема таблиця `NotificationSettings`
- ✅ Створено endpoints:
  - ✅ `GET /api/profile` - отримання профілю
  - ✅ `PUT /api/profile` - оновлення профілю (ім'я, тип, група, система)
  - ⏳ `GET /api/profile/stats` - статистика (для майбутньої реалізації)

### Frontend: ✅ ЗАВЕРШЕНО

- ✅ Створено екран профілю в `app/(tabs)/profile.tsx`
- ✅ Форма редагування профілю:
  - ✅ Редагування імені
  - ✅ Вибір типу користувача (`@react-native-picker/picker`)
  - ✅ Вибір групи ФОП (тільки для ФОП)
  - ✅ Вибір системи оподаткування (тільки для юросіб)
  - ✅ UX покращення: зелений текст статусу, адаптивна висота picker, відступи
- ✅ Налаштування сповіщень (модальне вікно):
  - ✅ Увімкнення/вимкнення дедлайн сповіщень
  - ✅ Увімкнення/вимкнення новинних сповіщень
  - ✅ Кнопка відправки тестового сповіщення
- ✅ Logout кнопка з підтвердженням

### Тестування: ✅ ПРОТЕСТОВАНО

- ✅ Протестовано створення/оновлення профілю
- ✅ Перевірено збереження налаштувань
- ✅ Перевірено відображення персоналізованого контенту
- ✅ Тестування на фізичному iOS пристрої успішне

---

## Етап 8: Push-уведомлення (Push Notifications) ✅ ЗАВЕРШЕНО

**Мета:** Реалізувати систему персоналізованих push-уведомлень на основі новин та календаря.

**Статус:** ✅ **ПОВНІСТЮ РЕАЛІЗОВАНО**

### Backend: ✅

- ✅ Налаштовано Expo Push Notifications сервіс (`exponent-server-sdk`)
- ✅ Створено модель `NotificationSettings` для налаштувань користувачів
- ✅ Розширено модель `User` (поле `push_token`)
- ✅ Створено Celery tasks:
  - ✅ `send_deadline_notifications` - відправка за 1 та 3 дні до дедлайну (щодня в 9:00)
  - ✅ `send_news_notifications` - персоналізовані новини 2 рази на тиждень (Пн, Чт в 10:00)
- ✅ Створено endpoints:
  - ✅ `POST /api/push/register` - реєстрація push token
  - ✅ `GET /api/push/settings` - отримання налаштувань
  - ✅ `PUT /api/push/settings` - оновлення налаштувань
  - ✅ `POST /api/push/test` - відправка тестового повідомлення
  - ✅ `DELETE /api/push/token` - видалення токена при logout
- ✅ Інтегровано OpenAI для вибору релевантних новостей (`NewsPersonalizationService`)
- ✅ Налаштовано Celery Beat для автоматичної відправки
- ✅ Створено міграцію БД

### Frontend: ✅

- ✅ Налаштовано `expo-notifications`
- ✅ Автоматичний запит дозволів при логіні
- ✅ Автоматична реєстрація push token на сервері
- ✅ Створено `NotificationHandler` для обробки уведомлень (foreground/background)
- ✅ Додано модальне вікно налаштувань у профілі користувача
- ✅ Реалізовано можливість включення/виключення типів уведомлень
- ✅ Додано кнопку відправки тестового повідомлення

### Тестування: ✅

- ✅ Документація створена (`PUSH_NOTIFICATIONS_GUIDE.md`)
- ✅ Інструкції з налаштування та тестування
- ✅ Можливість тестування через UI та Swagger
- ✅ Персоналізація працює на основі профілю користувача

---

## Етап 9: Форум (Community Forum)

**Мета:** Створити форум для спілкування користувачів, обміну досвідом та порадами.

### Backend:

- Створити моделі БД:
  - `forum_categories` (ФОП 1/2/3, ПДВ, Зарплата тощо)
  - `forum_threads` (топіки)
  - `forum_posts` (повідомлення)
  - `forum_likes` (лайки)
- Створити endpoints:
  - `GET /api/forum/categories` - список категорій
  - `GET /api/forum/threads` - список топіків з фільтрами
  - `POST /api/forum/threads` - створення топіку
  - `GET /api/forum/threads/{id}` - деталі топіку + коментарі
  - `POST /api/forum/posts` - створення коментаря
  - `POST /api/forum/likes` - лайк/дизлайк
  - `PUT /api/forum/posts/{id}/best-answer` - відмітка найкращої відповіді
- Налаштувати Elasticsearch індекси для пошуку по форуму
- Базова модерація (можливість видалення власних постів)

### Frontend:

- Створити таб/екран форуму
- Список категорій
- Список топіків з фільтрами (нові, гарячі, без відповіді)
- Екран створення топіку
- Екран перегляду топіку з коментарями (вкладені до 3 рівнів)
- Можливість лайкати та коментувати
- Пошук по форуму

### Тестування:

- Створити кілька тестових категорій та топіків
- Протестувати створення топіку та коментування
- Перевірити лайки
- Протестувати пошук
- Перевірити вкладені коментарі

---

## Етап 10: Фінальна інтеграція та поліпшення

**Мета:** Зв'язати всі частини разом, додати поліпшення UX, оптимізувати продуктивність.

### Інтеграція:

- Персоналізація новин на основі профілю користувача
- Фільтрація календарних подій за типом користувача
- Інтеграція пошуку з форумом (пошук по всіх розділах)
- Додати аналітику подій (amplitude/mixpanel або власне рішення)

### Поліпшення:

- Додати offline mode де це можливо (календар, калькулятори)
- Оптимізувати завантаження списків (infinite scroll, pagination)
- Додати skeleton loaders
- Покращити error handling та user feedback
- Додати onboarding для нових користувачів

### Тестування:

- E2E тестування основних user flows
- Тестування на різних пристроях (iOS/Android)
- Performance testing (навантаження на backend)
- Security audit (основні вразливості)

---

## Етап 11: Підготовка до релізу

**Мета:** Підготувати застосунок до публікації в App Store та Google Play.

### Mobile App:

- Налаштувати production builds (EAS Build)
- Додати splash screen та іконки
- Налаштувати deep linking
- Оптимізація розміру bundle
- Додати privacy policy та terms of service

### Backend:

- Налаштувати production environment на EC2
- Налаштувати моніторинг (Sentry для помилок)
- Налаштувати логування (CloudWatch або ELK)
- Backup стратегія для БД
- Rate limiting для API endpoints
- SSL certificates (Let's Encrypt)

### Документація:

- API документація (FastAPI Swagger/ReDoc)
- README з інструкціями для deployment
- Документація для майбутньої команди

### Тестування:

- Фінальне manual testing всіх фіч
- Beta testing з реальними користувачами
- Збір feedback та виправлення критичних багів

