# Celery Configuration - Автоматический парсинг новостей

## 📋 Текущая конфигурация

### ✅ Что настроено:

#### 1. Celery Worker
- **Статус**: ✅ Запущен
- **Контейнер**: `buhassistant_celery_worker`
- **Очереди**: `crawler`, `default`

#### 2. Celery Beat (Планировщик)
- **Статус**: ✅ Запущен
- **Контейнер**: `buhassistant_celery_beat`
- **Timezone**: `Europe/Kyiv`

#### 3. Redis (Брокер сообщений)
- **Статус**: ✅ Работает
- **URL**: `redis://redis:6379/0`

---

## 🤖 Доступные задачи:

### 1. `crawl_minfin_news_task`
**Что делает**:
- Парсит новости с minfin.com.ua (2 страницы)
- Фильтрует через OpenAI API
- Сохраняет релевантные в БД (только новые, без дубликатов)

**Результат**: ~200 спарсено → ~4 релевантных → ~4 сохранено

---

### 2. `crawl_liga_net_news_task`
**Что делает**:
- Парсит новости с liga.net
- Фильтрует через OpenAI API
- Сохраняет релевантные в БД (только новые, без дубликатов)

**Результат**: ~55 спарсено → ~5 релевантных → ~5 сохранено

---

### 3. `crawl_all_news_sources_task` ⭐ (ОСНОВНАЯ)
**Что делает**:
- Запускает ВСЕ краулеры последовательно:
  1. Minfin.com.ua
  2. Liga.net
- Для каждого источника:
  - Парсит новости
  - Фильтрует через OpenAI API
  - Сохраняет только новые релевантные новости
- Возвращает итоговую статистику

**Результат**: ~255 спарсено → ~9 релевантных → ~9 сохранено (на первый запуск)

**Время выполнения**: ~60-90 секунд (включая OpenAI)

---

## ⏰ Расписание (Celery Beat Schedule):

### Текущее расписание:

```python
'crawl-all-news-twice-daily': {
    'task': 'crawl_all_news_sources_task',
    'schedule': crontab(minute=0, hour='8,20'),  # 8:00 и 20:00
    'options': {'queue': 'crawler'}
}
```

**Когда запускается**: 
- **Утром**: 8:00 (Киевское время)
- **Вечером**: 20:00 (Киевское время)

**Что происходит**:
1. 8:00 - Парсит ВСЕ источники (minfin + liga.net)
2. OpenAI фильтрует релевантные новости
3. Сохраняет только новые в БД
4. 20:00 - Повторяет процесс

---

## 📊 Логика работы:

### Полный цикл:
```
1. Celery Beat (8:00 или 20:00)
   ↓
2. Запускает crawl_all_news_sources_task
   ↓
3. Парсит Minfin (200 новостей)
   ↓
4. OpenAI фильтрует (4 релевантных)
   ↓
5. Проверяет дубликаты по URL
   ↓
6. Сохраняет только новые
   ↓
7. Парсит Liga.net (55 новостей)
   ↓
8. OpenAI фильтрует (5 релевантных)
   ↓
9. Проверяет дубликаты по URL
   ↓
10. Сохраняет только новые
   ↓
11. Возвращает итоговую статистику
```

### Проверка дубликатов:
```python
# Перед сохранением проверяем URL
existing = db.query(News).filter(News.url == article['url']).first()

if existing:
    logger.info("⏭️ Skipping duplicate")
    skipped_count += 1
    continue

# Сохраняем только новые
db.add(news_item)
saved_count += 1
```

---

## 🚀 Как использовать:

### Запустить задачу вручную (для тестирования):

```bash
# Через Docker Compose
docker-compose exec backend python3 -c "
from app.tasks.crawler_tasks import crawl_all_news_sources_task
result = crawl_all_news_sources_task()
print(result)
"

# Или через Celery (асинхронно)
docker-compose exec celery_worker celery -A app.celery_app:celery_app call crawl_all_news_sources_task
```

### Проверить статус Celery:

```bash
# Список worker'ов
docker-compose exec celery_worker celery -A app.celery_app:celery_app inspect active

# Список запланированных задач
docker-compose exec celery_beat celery -A app.celery_app:celery_app inspect scheduled

# Статистика
docker-compose exec celery_worker celery -A app.celery_app:celery_app inspect stats
```

### Посмотреть логи:

```bash
# Celery Worker логи
docker-compose logs -f celery_worker

# Celery Beat логи
docker-compose logs -f celery_beat

# Последние 100 строк
docker-compose logs celery_worker --tail 100
```

---

## 📈 Статистика и мониторинг:

### После каждого запуска:

**Логи содержат**:
```
================================================================================
🕷️ Starting FULL NEWS CRAWL at 2025-11-03T08:00:00
================================================================================

📰 [1/2] Crawling Minfin.com.ua...
✅ Minfin: {'parsed': 200, 'filtered': 4, 'saved': 3, 'skipped': 1}

📰 [2/2] Crawling Liga.net...
✅ Liga.net: {'parsed': 55, 'filtered': 5, 'saved': 4, 'skipped': 1}

================================================================================
🎉 FULL NEWS CRAWL COMPLETED
   Total parsed: 255
   Total filtered by OpenAI: 9
   Total saved to DB: 7
   Total skipped (duplicates): 2
================================================================================
```

### API endpoints для проверки:

```bash
# Последние 10 новостей
curl "http://localhost:8000/api/news/?limit=10"

# Статистика по новостям
curl "http://localhost:8000/api/news/stats"

# Категории
curl "http://localhost:8000/api/news/categories"
```

---

## ⚙️ Конфигурация Celery:

### Файл: `backend/app/celery_app.py`

```python
celery_app = Celery(
    "buhassistant",
    broker=settings.REDIS_URL,              # redis://redis:6379/0
    backend=settings.REDIS_URL,
    include=['app.tasks.crawler_tasks']
)

celery_app.conf.update(
    task_serializer='json',
    timezone='Europe/Kyiv',                 # Киевское время
    task_time_limit=30 * 60,                # 30 минут максимум
)

# Расписание
celery_app.conf.beat_schedule = {
    'crawl-all-news-twice-daily': {
        'task': 'crawl_all_news_sources_task',
        'schedule': crontab(minute=0, hour='8,20'),  # 8:00 и 20:00
        'options': {'queue': 'crawler'}
    },
}
```

---

## 🔧 Troubleshooting:

### Проблема: Задачи не запускаются автоматически

**Решение**:
```bash
# 1. Проверить что Celery Beat запущен
docker-compose ps

# 2. Удалить старый schedule файл
docker-compose exec celery_beat rm -f /app/celerybeat-schedule

# 3. Перезапустить Celery Beat
docker-compose restart celery_beat

# 4. Проверить логи
docker-compose logs celery_beat --tail 50
```

### Проблема: Ошибки при выполнении задач

**Решение**:
```bash
# 1. Проверить логи worker
docker-compose logs celery_worker --tail 100

# 2. Перезапустить worker
docker-compose restart celery_worker

# 3. Проверить что БД доступна
docker-compose exec backend python3 -c "from app.db.database import engine; print(engine.connect())"
```

### Проблема: OpenAI API ошибки

**Решение**:
```bash
# 1. Проверить API ключ
docker-compose exec backend python3 -c "from app.core.config import settings; print(f'OpenAI key: {settings.OPENAI_API_KEY[:20]}...')"

# 2. Проверить баланс на https://platform.openai.com/account/usage

# 3. Временно отключить фильтрацию (использовать моки) в news_filter.py
```

---

## 📝 Файлы конфигурации:

1. **Celery app**: `backend/app/celery_app.py`
2. **Celery tasks**: `backend/app/tasks/crawler_tasks.py`
3. **Docker Compose**: `docker-compose.yml` (секции celery_worker, celery_beat)
4. **Settings**: `backend/app/core/config.py`

---

## 🎯 Следующие шаги:

1. ✅ **Готово**: Celery Worker и Beat запущены
2. ✅ **Готово**: Расписание настроено (2 раза в день)
3. ✅ **Готово**: Задачи работают (minfin + liga.net)
4. ✅ **Готово**: OpenAI фильтрация интегрирована
5. ✅ **Готово**: Проверка дубликатов реализована
6. ⏳ **Опционально**: Добавить уведомления в Telegram при ошибках
7. ⏳ **Опционально**: Добавить метрики и мониторинг (Prometheus/Grafana)

---

## 📊 Ожидаемые результаты:

### Первый запуск (8:00):
- Спарсено: ~255 новостей
- Отфильтровано: ~9 релевантных
- Сохранено: ~9 (все новые)
- Пропущено: 0

### Второй запуск (20:00):
- Спарсено: ~255 новостей
- Отфильтровано: ~9 релевантных
- Сохранено: ~5 (только новые)
- Пропущено: ~4 (дубликаты с утра)

### Следующий день (8:00):
- Спарсено: ~255 новостей
- Отфильтровано: ~9 релевантных
- Сохранено: ~7-8 (новые за ночь)
- Пропущено: ~1-2 (дубликаты)

---

## ✅ Итог:

**Celery полностью настроен и работает!** 🚀

- ✅ Автоматический парсинг 2 раза в день (8:00, 20:00)
- ✅ Фильтрация через OpenAI
- ✅ Сохранение только релевантных новостей
- ✅ Проверка дубликатов по URL
- ✅ Логирование всех операций
- ✅ Статистика после каждого запуска

**На фронтенде** пользователь всегда видит свежие новости благодаря автоматическому обновлению!

