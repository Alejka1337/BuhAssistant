# ⚡ Быстрый Чеклист для Тестирования

## 🚀 Запуск (5 минут)

### 1. Запустите Backend
```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
✅ Должен показать: `Uvicorn running on http://0.0.0.0:8000`

### 2. Запустите Frontend
```bash
# В новом терминале, в корне проекта
npx expo start
```
✅ Нажмите `i` для iOS или `a` для Android

---

## 🧪 Тест 1: Проверка 401 Unauthorized (ГЛАВНОЕ!)

### Шаги:
1. ✅ Откройте приложение
2. ✅ Нажмите "Зареєструватися"
3. ✅ Введите:
   - Email: ваш реальный email
   - Пароль: `test12345`
   - Подтвердите пароль: `test12345`
4. ✅ Нажмите "Зареєструватися"
5. ✅ **Должен открыться экран верификации** (без ошибок)
6. ✅ Проверьте почту - должно прийти письмо с кодом
7. ✅ Введите код из письма
8. ✅ Нажмите "Підтвердити"

### ✅ Ожидаемый результат:
- **НЕТ ошибки 401 Unauthorized**
- Появляется сообщение "Ваш email успішно підтверджено!"
- Переход на главный экран приложения

### ❌ Если не работает:
- Проверьте логи бекенда
- Убедитесь, что используете правильный код
- Попробуйте "Відправити код знову"

---

## 🧪 Тест 2: Клавиатура на iOS

### Шаги:
1. ✅ Откройте приложение на **iOS симуляторе**
2. ✅ Перейдите на экран верификации (как в Тест 1)
3. ✅ Попробуйте:
   - Ввести цифры по одной → должно автоматически переключаться
   - Нажать Backspace → должно вернуться назад
   - Вставить код целиком (если есть) → должно заполнить все поля

### ✅ Ожидаемый результат:
- **Клавиатура НЕ зависает**
- Плавное переключение между полями
- Нет лагов при вводе

### ❌ Если зависает:
- Сообщите об этом - нужна дополнительная настройка
- Попробуйте перезапустить симулятор

---

## 🧪 Тест 3: API Endpoint (для разработчиков)

### Шаги:
```bash
# 1. Регистрация
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test123@example.com","password":"test12345"}'

# 2. Проверьте email и получите код

# 3. Верификация (замените КОД_ИЗ_ПИСЬМА на реальный код)
curl -X POST http://localhost:8000/api/auth/verify \
  -H "Content-Type: application/json" \
  -d '{"email":"test123@example.com","code":"КОД_ИЗ_ПИСЬМА"}'
```

### ✅ Ожидаемый результат:
```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "user": {
    "is_verified": true  // ← ВАЖНО!
  }
}
```

### ❌ Если ошибка:
- Проверьте, что бекенд запущен
- Проверьте правильность email и кода
- Проверьте логи бекенда

---

## 📋 Быстрая проверка логов

### Backend (терминал с uvicorn):
Должны видеть:
```
INFO: "POST /api/auth/register HTTP/1.1" 201 Created
INFO: "POST /api/auth/verify HTTP/1.1" 200 OK    ← ВАЖНО!
```

### Frontend (Metro bundler):
Должны видеть:
```
Verifying email: your@email.com
Response status: 200    ← ВАЖНО!
```

---

## ✅ Итоговый Чеклист

- [ ] Backend запущен
- [ ] Frontend запущен
- [ ] Регистрация работает
- [ ] Письмо с кодом приходит
- [ ] Верификация работает БЕЗ ошибки 401
- [ ] Клавиатура не зависает на iOS
- [ ] Успешный переход на главный экран

---

## 🎉 Если все работает:

**Поздравляю! Исправления работают корректно!**

Можете:
1. Удалить тестовые аккаунты из БД (если нужно)
2. Продолжить разработку других фич
3. Сделать commit изменений

---

## 🐛 Если что-то не работает:

1. **Проверьте логи** (backend и frontend)
2. **См. подробную инструкцию:** `TESTING_EMAIL_VERIFICATION.md`
3. **См. описание проблем:** `doc-files/EMAIL_VERIFICATION_FIX.md`
4. **Проверьте изменённые файлы:**
   - `backend/app/api/auth.py`
   - `backend/app/schemas/auth.py`
   - `utils/authService.ts`
   - `app/verify-email.tsx`

---

## 🔄 Откат изменений (если нужно)

Если исправления вызвали новые проблемы:
```bash
git diff HEAD  # Посмотреть изменения
git checkout -- <файл>  # Откатить конкретный файл
git stash  # Отложить все изменения
```

---

**Время тестирования:** ~5-10 минут  
**Сложность:** ⭐⭐ (2/5)  
**Приоритет:** 🔴 ВЫСОКИЙ (основная функциональность)

