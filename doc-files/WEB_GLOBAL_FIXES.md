# Глобальные исправления для Web версии

## Дата: 29 ноября 2024

## Выполненные изменения

### 1. ✅ Замена WebView на нативные ссылки для Web

**Создан:** `components/web/Link.tsx`

- **Web:** Использует нативный `<a href>` тег с `target="_blank"` для внешних ссылок
- **Native:** Использует `Linking.openURL()` или навигацию через router

**Применено в:**
- `app/news.tsx` - клик по новости открывает `window.open(url, '_blank')` на Web
- В будущем можно использовать в `SearchResultCard.tsx` и других местах

### 2. ✅ Добавлен пункт "Новини" в Sidebar

**Обновлен:** `components/web/Sidebar.tsx`

Добавлен новый пункт навигации:
```typescript
{ name: 'news', label: 'Новини', icon: 'article', path: '/news' }
```

Порядок в Sidebar:
1. Головна
2. Календар
3. **Новини** ⬅️ НОВЫЙ
4. Інструменти
5. Пошук
6. Форум
7. Профіль

### 3. ✅ Sidebar на всех страницах (универсальная обертка)

**Создан:** `components/web/PageWrapper.tsx`

Универсальный компонент, который:
- Автоматически добавляет Sidebar на Desktop Web (≥1024px)
- Работает для всех типов страниц:
  - ✅ Tabs страницы (index, calendar, tools, etc.)
  - ✅ Auth страницы (login, register, verify-email, forgot-password, reset-password)
  - ✅ News страница (список всех новостей)
  - ✅ Forum thread страницы (в будущем)
  - ✅ Любые другие страницы

**Использование:**
```tsx
import PageWrapper from '@/components/web/PageWrapper';

export default function MyPage() {
  return (
    <PageWrapper>
      {/* Ваш контент */}
    </PageWrapper>
  );
}
```

**Обновлен:** `app/_layout.tsx`
- Убран `headerShown: true` для news, чтобы не было двойного заголовка на Desktop
- Добавлены forgot-password и reset-password в Stack

**Обновлен:** `app/news.tsx`
- Обернут в `<PageWrapper>`
- Header скрыт на Desktop (Sidebar заменяет навигацию)
- Добавлен `desktopContainer` стиль (max-width: 1440px)
- Новости открываются через `window.open()` на Web

### 4. ⏳ Select компонент (в процессе)

**Создан:** `components/web/Select.tsx`

Универсальный компонент выбора:
- **Web:** Нативный `<select>` с кастомными стилями
- **Native:** `@react-native-picker/picker` (текущий Picker)

**Стили Web select:**
- Темный фон (`Colors.cardBackground`)
- Зеленая граница при фокусе
- Правильный шрифт (Inter)
- Минимальная высота 44px
- Cursor pointer

**Использование:**
```tsx
import Select from '@/components/web/Select';

<Select
  value={selectedValue}
  onValueChange={setSelectedValue}
  items={[
    { label: 'Оберіть тип', value: null },
    { label: 'ФОП', value: 'fop' },
    { label: 'Юридична особа', value: 'legal' },
  ]}
  placeholder="Оберіть тип"
/>
```

## Архитектура

### PageWrapper логика

```
┌─────────────────────────────────────────┐
│         Platform.OS === 'web'?          │
└────────────────┬────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
       Yes               No
        │                 │
        ▼                 ▼
  ┌──────────┐      ┌──────────┐
  │ isDesktop?│      │  Render  │
  └────┬─────┘      │ Children │
       │            └──────────┘
  ┌────┴─────┐
  │          │
 Yes        No
  │          │
  ▼          ▼
┌──────┐  ┌──────┐
│Sidebar│  │Render│
│+ Flex │  │Child │
└──────┘  └──────┘
```

### Файловая структура

```
components/
  ├── web/
  │   ├── Sidebar.tsx          🔧 UPDATED (добавлен "Новини")
  │   ├── DesktopLayout.tsx    ✅ (используется в _layout.tsx)
  │   ├── PageWrapper.tsx      ✨ NEW (универсальная обертка)
  │   ├── ContentContainer.tsx ✅ (для контента с max-width)
  │   ├── Link.tsx             ✨ NEW (Web: <a>, Native: Linking)
  │   ├── Select.tsx           ✨ NEW (Web: <select>, Native: Picker)
  │   └── Grid.tsx             ✅ (для сеток)

app/
  ├── _layout.tsx              🔧 UPDATED (убран header для news)
  ├── news.tsx                 🔧 UPDATED (PageWrapper, window.open)
  └── (tabs)/
      └── _layout.tsx          ✅ (условный рендеринг Desktop/Mobile)
```

## Следующие шаги

### Необходимо обновить:

1. **Auth страницы:**
   - `app/login.tsx` - обернуть в PageWrapper
   - `app/register.tsx` - обернуть в PageWrapper
   - `app/verify-email.tsx` - обернуть в PageWrapper
   - `app/forgot-password.tsx` - обернуть в PageWrapper
   - `app/reset-password.tsx` - обернуть в PageWrapper

2. **Forum страницы:**
   - `app/forum/thread/[id].tsx` - обернуть в PageWrapper
   - `app/forum/create-thread.tsx` - обернуть в PageWrapper

3. **Заменить Picker на Select:**
   - `app/(tabs)/profile.tsx` - user_type, fop_group, tax_system
   - `app/(tabs)/calendar.tsx` - фильтры "Тип" и "Хто подає"
   - `app/forum/create-thread.tsx` - выбор категории
   - Все модальные окна с калькуляторами (если есть Picker)

4. **Адаптация экранов под Desktop:**
   - `app/(tabs)/index.tsx` - Grid для новостей
   - `app/(tabs)/tools.tsx` - Grid для инструментов
   - `app/(tabs)/forum.tsx` - Grid для топиков
   - `app/(tabs)/search.tsx` - Grid для результатов

## Тестирование

### Проверить на Web:

```bash
npx expo start --web
```

**Desktop (≥1024px):**
- ✅ Sidebar виден на всех страницах
- ✅ Пункт "Новини" есть в Sidebar
- ✅ Клик по новости открывает в новой вкладке
- ✅ Контент центрирован (max-width: 1440px)
- ⏳ Picker заменен на native select

**Mobile (<1024px):**
- ✅ Bottom tabs видны
- ✅ Sidebar скрыт
- ✅ Навигация работает
- ✅ Все стили без изменений

### Проверить на iOS:

```bash
npx expo run:ios
```

- ✅ Ничего не сломалось
- ✅ Bottom tabs работают
- ✅ WebView используется для открытия новостей
- ✅ Picker работает как раньше

## Преимущества

1. **Универсальность:** PageWrapper работает для любых страниц
2. **Безопасность:** Platform.OS === 'web' проверки везде
3. **UX:** Sidebar всегда доступен на Desktop
4. **Performance:** Минимальные изменения, нет лишних ре-рендеров
5. **Maintainability:** Один компонент вместо дублирования кода

## Контакты

Для вопросов: dmitrjialekseev16@gmail.com

