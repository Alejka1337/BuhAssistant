# 🔔 Гайд по тестированию Push Notifications

## ✅ Что было сделано

### 1. Backend
- ✅ API эндпоинты для регистрации токенов (`/api/push/register`)
- ✅ API эндпоинт для отправки тестовых уведомлений (`/api/push/test`)
- ✅ API эндпоинты для управления настройками (`/api/push/settings`)
- ✅ Логирование для отладки
- ✅ Celery tasks для автоматических уведомлений (дедлайны, новости)

### 2. Frontend
- ✅ Регистрация push токена при входе
- ✅ Запрос разрешений у пользователя
- ✅ Отправка токена на бэкенд
- ✅ UI для управления настройками уведомлений
- ✅ Кнопка для отправки тестового уведомления

### 3. Конфигурация
- ✅ `app.json`: настроены уведомления и APNs
- ✅ `eas.json`: профили для сборки
- ✅ Apple Developer Console: создан APNs Key
- ✅ `Info.plist`: обновлен buildNumber

### 4. Исправления
- ✅ Раскомментирована регистрация push токена в `AuthContext.tsx`
- ✅ Добавлен плагин `expo-notifications` в `app.json`
- ✅ Добавлено логирование на бэкенде

---

## 🧪 Как протестировать

### Шаг 1: Пересобрать приложение

**ВАЖНО:** После изменений в `app.json` нужно пересобрать приложение!

```bash
cd /Users/alejka1337/Desktop/buhassistant
eas build --platform ios --profile production
```

**Ожидаемое время:** 15-20 минут.

После сборки:
```bash
eas submit --platform ios --profile production
```

Выбрать самый свежий билд (Build number: 3).

---

### Шаг 2: Установить новую версию на iPhone

1. Дождаться обработки в App Store Connect (~5-10 минут)
2. Открыть TestFlight на iPhone
3. Обновить приложение до новой версии

---

### Шаг 3: Проверить регистрацию push токена

1. **Удалить приложение** с iPhone (если уже установлено)
2. Установить заново через TestFlight
3. Открыть приложение
4. Войти в аккаунт
5. При первом входе должен появиться запрос:
   ```
   "eGlavBuh" Would Like to Send You Notifications
   [Don't Allow] [Allow]
   ```
6. Нажать **Allow**

**Проверить логи бэкенда:**
```bash
docker-compose logs -f backend
```

Должно быть:
```
Push token registered for user <user_id>
```

---

### Шаг 4: Отправить тестовое уведомление

1. В приложении: **Профіль** → **Сповіщення**
2. Нажать **"Надіслати тестове повідомлення"**
3. Должно появиться:
   - **Alert:** "Успіх! Тестове повідомлення надіслано!"
   - **Push уведомление** на экране iPhone: "Тестове повідомлення: Це тестове push-повідомлення з eGlavBuh 🎉"

**Логи бэкенда должны показать:**
```
Test notification request: title='Тестове повідомлення' body='Це тестове push-повідомлення з eGlavBuh 🎉'
Current user push_token: ExponentPushToken[XXXXXXXXXX]
Push notification sent successfully to 1 recipients
```

---

## 🐛 Troubleshooting

### Проблема 1: "Не вдалось надіслати тестове повідомлення"

**Причина:** Push токен не зарегистрирован.

**Решение:**
1. Проверить логи бэкенда:
   ```
   User <user_id> has no push token registered
   ```
2. Выйти из приложения
3. Войти снова
4. Разрешить уведомления

---

### Проблема 2: Токен не регистрируется

**Проверить:**
1. В `AuthContext.tsx` раскомментирован код регистрации токена
2. В `app.json` добавлен плагин `expo-notifications`
3. В `app.json` установлен `ios.config.usesApns: true`
4. Приложение собрано с новой конфигурацией (EAS Build)

---

### Проблема 3: Push токен в БД, но уведомление не приходит

**Проверить:**
1. **APNs Key в EAS:**
   ```bash
   eas credentials -p ios
   ```
   Должен быть Apple Push Notifications service key.

2. **Expo Push Token формат:**
   Должен начинаться с `ExponentPushToken[...]`

3. **Логи Expo Push Service:**
   В ответе от бэкенда должно быть:
   ```json
   {
     "success": true,
     "push_results": [
       {
         "status": "ok",
         "id": "XXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"
       }
     ]
   }
   ```

   Если `"status": "error"`, проверить:
   - `"message": "DeviceNotRegistered"` → токен невалидный
   - `"message": "InvalidCredentials"` → проблема с APNs Key

---

### Проблема 4: Уведомление приходит, но не отображается

**iOS особенности:**
- Если приложение **открыто** → уведомление показывается только в foreground (в виде баннера сверху)
- Если приложение **закрыто** → уведомление показывается как обычно (в центре уведомлений)
- Если приложение **в фоне** → уведомление показывается в центре уведомлений

**Решение:**
1. Свернуть или закрыть приложение
2. Отправить тестовое уведомление
3. Проверить центр уведомлений (свайп вниз)

---

## 📊 Проверка базы данных

Проверить, что токен записался в БД:

```bash
docker-compose exec backend python -c "
from app.db.database import get_db
from app.models.user import User

db = next(get_db())
user = db.query(User).filter(User.email == 'ВАШ_EMAIL').first()
print(f'User: {user.email}')
print(f'Push Token: {user.push_token}')
"
```

Должно вывести:
```
User: ваш@email.com
Push Token: ExponentPushToken[XXXXXXXXXXXXXXXXXX]
```

---

## 🎯 Что дальше

После успешного теста:

### 1. Протестировать автоматические уведомления

**Дедлайны (тестирование):**
1. Добавить тестовую отчетность в `all.json` на завтра
2. Подождать когда Celery задача сработает
3. Проверить уведомление

**Новости (тестирование):**
1. Настроить профиль (тип пользователя, ФОП группа)
2. Подождать запланированного времени отправки (понедельник/четверг)
3. Проверить персонализированные новости

### 2. Оптимизация

- [ ] Настроить звуки для уведомлений
- [ ] Добавить категории уведомлений
- [ ] Реализовать deep linking (открытие конкретной страницы по клику на уведомление)
- [ ] Добавить badge count

### 3. Мониторинг

- [ ] Логировать все отправки уведомлений
- [ ] Отслеживать failed deliveries
- [ ] Статистика по click-through rate

---

## 📚 Полезные ссылки

- [Expo Push Notifications](https://docs.expo.dev/push-notifications/overview/)
- [EAS Build APNs Configuration](https://docs.expo.dev/push-notifications/push-notifications-setup/)
- [Expo Push Token Format](https://docs.expo.dev/versions/latest/sdk/notifications/#expopushtokenobject)
- [Apple Push Notification Service](https://developer.apple.com/documentation/usernotifications)

---

## ✅ Checklist для релиза

- [ ] Push notifications работают на физическом устройстве
- [ ] Тестовое уведомление отправляется и приходит
- [ ] Автоматические уведомления о дедлайнах работают
- [ ] Персонализированные новости работают
- [ ] Настройки уведомлений сохраняются
- [ ] Уведомления отключаются по настройкам
- [ ] Push токен удаляется при logout

---

## 🎉 Успехов с тестированием!

Если что-то не работает - проверьте логи бэкенда и фронтенда, они должны дать подсказку!

