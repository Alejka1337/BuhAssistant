# 🧪 Google OAuth Testing Guide

## ✅ Статус: Готово к тестированию!

### Что сделано:
- ✅ Backend перезапущен с Google credentials
- ✅ Google OAuth endpoints доступны (`/api/auth/google`, `/api/auth/google/url`)
- ✅ iOS приложение пересобрано с нативными модулями
- ✅ iOS симулятор запускается

---

## 🎯 Тестовый сценарий

### Сценарий 1: Регистрация через Google (новый пользователь)

**Шаги:**
1. Откройте приложение в iOS симуляторе
2. Перейдите на таб **"Профіль"**
3. Нажмите кнопку **"Увійти"**
4. На экране Login нажмите синюю кнопку **"Увійти через Google"**
5. Выберите Google аккаунт (используйте test user из Google Cloud Console)
6. Подтвердите доступ

**Ожидаемый результат:**
- ✅ Откроется Google Sign In modal
- ✅ После выбора аккаунта - автоматический вход
- ✅ Переход на главную страницу (/(tabs))
- ✅ В профиле отображается:
  - Имя из Google
  - Email из Google
  - `is_verified: true`

**Проверка в БД:**
```bash
docker-compose exec postgres psql -U buhassistant -d buhassistant_db -c "SELECT id, email, full_name, google_id, is_verified, hashed_password FROM users ORDER BY id DESC LIMIT 1;"
```

Должно быть:
- `email` - ваш Google email
- `full_name` - ваше имя из Google
- `google_id` - заполнен (Google User ID)
- `is_verified` - `true`
- `hashed_password` - `NULL` (нет пароля)

---

### Сценарий 2: Вход через Google (существующий email)

**Подготовка:**
1. Зарегистрируйте пользователя через обычную форму (Register):
   - Email: ваш Google email
   - Password: test123456
   - Full Name: Test User
2. Выйдите из приложения (Logout)

**Шаги:**
1. Нажмите "Увійти"
2. Нажмите "Увійти через Google"
3. Выберите тот же Google аккаунт

**Ожидаемый результат:**
- ✅ Успешный вход
- ✅ Система связала существующий аккаунт с Google
- ✅ Теперь можно входить как через email/password, так и через Google

**Проверка в БД:**
```bash
docker-compose exec postgres psql -U buhassistant -d buhassistant_db -c "SELECT id, email, google_id, hashed_password FROM users WHERE email='ваш_email';"
```

Должно быть:
- `google_id` - теперь заполнен
- `hashed_password` - НЕ NULL (пароль сохранен)

---

### Сценарий 3: Выход и повторный вход

**Шаги:**
1. После входа через Google перейдите в Профиль
2. Нажмите "Вийти з облікового запису"
3. Подтвердите
4. Снова нажмите "Увійти"
5. Нажмите "Увійти через Google"

**Ожидаемый результат:**
- ✅ Быстрый вход (Google может помнить выбор аккаунта)
- ✅ Токены обновлены в SecureStore

---

### Сценарий 4: Переключение между аккаунтами

**Шаги:**
1. Войдите через Google (аккаунт A)
2. Выйдите
3. Войдите через email/password (другой аккаунт B)
4. Проверьте профиль

**Ожидаемый результат:**
- ✅ Правильное переключение между аккаунтами
- ✅ Данные в профиле соответствуют текущему пользователю

---

## 🐛 Troubleshooting

### Ошибка: "Google Sign In ще не налаштовано"

**Причина:** Client IDs не найдены в `app.json`

**Решение:**
```bash
cat app.json | grep google
```

Должно быть:
```json
"googleWebClientId": "914514821616-47musasu3ster3fjvjlbehc8fdrdgbno.apps.googleusercontent.com",
"googleIosClientId": "914514821616-47musasu3ster3fjvjlbehc8fdrdgbno.apps.googleusercontent.com"
```

Если нет - добавьте и пересоберите:
```bash
npx expo prebuild --clean
npx expo run:ios
```

---

### Ошибка: "Invalid Google token"

**Причина:** Backend не может верифицировать токен

**Проверка:**
```bash
# Проверьте GOOGLE_CLIENT_ID в backend/.env
docker-compose exec backend env | grep GOOGLE
```

Должно быть:
```
GOOGLE_CLIENT_ID=914514821616-rh81j21a2qbqu104j45j6j09661jo6qm.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxxxxxxx
```

**⚠️ Важно:** `GOOGLE_CLIENT_ID` должен быть **Web Client ID** (не iOS)!

**Решение:**
1. Откройте `backend/.env`
2. Убедитесь, что `GOOGLE_CLIENT_ID` - это Web Client ID
3. Перезапустите backend:
   ```bash
   docker-compose restart backend
   ```

---

### Ошибка: "statusCode.SIGN_IN_CANCELLED"

**Причина:** Пользователь отменил вход

**Решение:** Это нормально, попробуйте снова

---

### Ошибка: "PLAY_SERVICES_NOT_AVAILABLE"

**Причина:** Google Play Services недоступны (только для Android)

**На iOS:** Игнорируйте, это не должно происходить

**На Android:** Убедитесь, что симулятор/эмулятор поддерживает Google Play Services

---

### Приложение не может подключиться к backend

**Проверка:**
```bash
# Из iOS симулятора localhost должен работать
curl http://localhost:8000/api/auth/health
```

**Если не работает:**
- Убедитесь, что backend запущен: `docker-compose ps`
- Проверьте `API_URL` в `constants/api.ts` (должно быть `http://localhost:8000`)

---

### Google Sign In modal не открывается

**Причина:** Нативные модули не установлены

**Решение:**
```bash
cd /Users/alejka1337/Desktop/buhassistant

# Удалить старые нативные файлы
rm -rf ios android

# Пересоздать
npx expo prebuild --clean

# Установить pods
npx pod-install

# Запустить
npx expo run:ios
```

---

## 📊 Проверка логов

### Backend логи:
```bash
cd /Users/alejka1337/Desktop/buhassistant/backend
docker-compose logs -f backend
```

При успешной авторизации через Google должно быть:
```
INFO: POST /api/auth/google - 200 OK
```

### iOS логи:
Смотрите в Xcode console или Metro bundler терминал

---

## ⚠️ Важные замечания

### 1. Test Users в Google Cloud Console

**Если вы видите ошибку "Access blocked: This app's request is invalid":**

1. Перейдите в Google Cloud Console
2. OAuth consent screen
3. Test users → Add users
4. Добавьте свой Google email
5. Save

**Пока приложение в режиме "Testing", вы можете войти ТОЛЬКО с test users!**

---

### 2. Client IDs в app.json

**⚠️ Обратите внимание:**

```json
{
  "googleWebClientId": "914514821616-47musasu3ster3fjvjlbehc8fdrdgbno.apps.googleusercontent.com",  // iOS Client ID
  "googleIosClientId": "914514821616-47musasu3ster3fjvjlbehc8fdrdgbno.apps.googleusercontent.com"    // iOS Client ID
}
```

**Оба поля используют iOS Client ID!** Это правильно для `@react-native-google-signin/google-signin`.

---

### 3. Backend .env

```bash
GOOGLE_CLIENT_ID=914514821616-rh81j21a2qbqu104j45j6j09661jo6qm.apps.googleusercontent.com  # Web Client ID
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxxxxxxx  # Web Client Secret
```

**Backend использует Web Client ID для верификации токенов!**

---

## 🎯 Checklist перед тестированием

- [x] Backend .env заполнен с Web Client ID и Secret
- [x] Backend перезапущен (`docker-compose restart backend`)
- [x] app.json содержит iOS Client IDs
- [x] Выполнен `npx expo prebuild --clean`
- [x] iOS приложение запущено (`npx expo run:ios`)
- [ ] Добавлен test user в Google Cloud Console (если еще не добавлен)

---

## 📸 Ожидаемый UI

### Экран Login:
```
┌─────────────────────────────────┐
│          [Иконка]               │
│           Вхід                  │
│  Увійдіть до свого облікового   │
│           запису                │
│                                 │
│  [Email input]                  │
│  [Password input]               │
│  [Увійти button]                │
│  Забули пароль?                 │
│                                 │
│  ────── або ──────              │
│                                 │
│  [Увійти через Google] (синяя) │ ← Эта кнопка!
│                                 │
│  Ще немає облікового запису?   │
│  [Зареєструватися]              │
│                                 │
│  Продовжити без входу           │
└─────────────────────────────────┘
```

### После успешного входа:
```
┌─────────────────────────────────┐
│          Профіль                │
│                                 │
│          [Иконка]               │
│       John Doe (из Google)      │
│    john@gmail.com               │
│                                 │
│  Інформація про обліковий запис │
│  ✓ Статус: Активний             │
│  ✓ Верифікація: Підтверджено    │
│  📅 Дата реєстрації: ...        │
│                                 │
│  [Вийти з облікового запису]    │
└─────────────────────────────────┘
```

---

## ✅ Успешное завершение теста

После успешного тестирования:

1. ✅ Пользователь может войти через Google
2. ✅ Данные из Google (имя, email) отображаются в профиле
3. ✅ `is_verified` автоматически `true`
4. ✅ Токены сохранены в SecureStore
5. ✅ При перезапуске приложения пользователь остается авторизованным
6. ✅ В БД создан пользователь с `google_id`

---

**Готово к тестированию! 🚀**

Дайте знать, если увидите какие-то ошибки!

